export interface Question {
  q: string;
  a: string;
}

export interface TechInfo {
  title: string;
  slug: string;
  icon: string;
  metaTitle: string;
  metaDesc: string;
  intro: string;
  questions: Question[];
  related: string[];
}

export const TECHS: Record<string, TechInfo> = {
  react: {
    title: "React Interview Questions",
    slug: "react",
    icon: "⚛️",
    metaTitle: "Top 50 React Interview Questions & Answers (2025)",
    metaDesc: "Crack your React interview with the top 50 most-asked React questions and answers for 2025. Covers hooks, Virtual DOM, Context, Redux, performance, and SSR.",
    intro: "React is the most in-demand frontend library in India's tech job market. Whether you're interviewing at a startup or FAANG, these are the questions you'll most likely face — asked at Swiggy, Razorpay, Flipkart, Meesho, and global companies.",
    related: ["javascript", "typescript", "nodejs"],
    questions: [
      { q: "What is React and what are its key features?", a: "React is a JavaScript library for building user interfaces, developed by Facebook. Its key features are the Virtual DOM for efficient updates, a component-based architecture for reusability, unidirectional data flow for predictable state management, JSX syntax for declarative UI, and hooks for state and side effects in functional components." },
      { q: "What is JSX and why do we use it?", a: "JSX (JavaScript XML) is a syntax extension that lets you write HTML-like markup inside JavaScript. Babel transforms it into React.createElement() calls at build time. JSX is optional but makes component code far more readable — <Button color='red'>Click</Button> is much clearer than React.createElement(Button, {color:'red'}, 'Click')." },
      { q: "What is the Virtual DOM and how does it improve performance?", a: "The Virtual DOM is a lightweight in-memory JavaScript representation of the real DOM. When state changes, React builds a new Virtual DOM tree, diffs it against the previous one (reconciliation), and applies only the changed nodes to the real DOM. This batched, minimal update strategy avoids expensive full DOM re-renders." },
      { q: "What are React Hooks? Name the most commonly used ones.", a: "Hooks are functions that let functional components use state and lifecycle features. The most common: useState (local state), useEffect (side effects), useContext (consume Context), useRef (mutable refs / DOM access), useMemo (memoize computed values), useCallback (memoize functions), and useReducer (complex state logic)." },
      { q: "What is the difference between useState and useReducer?", a: "useState is ideal for simple, independent state values. useReducer is better when the next state depends on the previous state or when multiple sub-values need coordinated updates. useReducer takes a reducer function and initial state, returning the current state and a dispatch function — similar to Redux but local to the component." },
      { q: "What is useEffect and how does the dependency array work?", a: "useEffect runs side effects after render. With no dependency array it runs after every render; with [] it runs once on mount; with specific values it runs when those values change. The optional cleanup function runs before the next effect and on unmount — use it to cancel subscriptions, timers, or fetch requests." },
      { q: "What is the difference between controlled and uncontrolled components?", a: "In a controlled component React state is the single source of truth for the input's value — every keystroke triggers setState via onChange. In an uncontrolled component the DOM manages the value and you read it via a ref when needed. Controlled components are easier to validate and test; uncontrolled components are simpler for quick one-off inputs." },
      { q: "What are props and state? How do they differ?", a: "Props are read-only data passed from parent to child — a component cannot modify its own props. State is mutable data owned by the component that, when changed via setState/useState, triggers a re-render. Data flows downward through props; to pass data upward you pass callback functions as props." },
      { q: "What is prop drilling and how do you avoid it?", a: "Prop drilling is threading props through multiple intermediate components that don't use them, just to reach a deeply nested child. Solutions: React Context API for low-frequency global data (theme, auth), a state manager like Redux or Zustand for complex apps, or component composition patterns that reduce nesting depth." },
      { q: "What is the React Context API?", a: "Context provides a way to share values between components without explicitly passing props at every level. You create a context with React.createContext(), provide it via a Provider component, and consume it anywhere in the tree with useContext(). Best for values that rarely change — frequent updates in Context cause every consumer to re-render." },
      { q: "What is React.memo and when should you use it?", a: "React.memo is a higher-order component that skips re-rendering a functional component if its props haven't changed (shallow comparison). Use it when a child component is expensive to render and its parent re-renders often. Don't use it everywhere — the comparison itself has a cost and shallow equality checks can miss nested object changes." },
      { q: "What is the difference between useMemo and useCallback?", a: "useMemo memoizes the return value of a function — the computation doesn't re-run unless dependencies change. useCallback memoizes the function reference itself. Use useMemo to avoid expensive recalculations; use useCallback to give a stable function reference to child components wrapped in React.memo, preventing them from re-rendering due to a new function instance each render." },
      { q: "What is useRef and how does it differ from useState?", a: "useRef returns a mutable { current } object that persists across renders. Mutating .current does NOT trigger a re-render — unlike useState. Common uses: holding a reference to a DOM element (for focus, scroll, measurements) or storing a mutable value like a timer ID or the previous state value." },
      { q: "Why are keys important in React lists?", a: "Keys are stable identifiers that help React track which list items changed, were added, or removed during reconciliation. Without keys, React re-renders all items on any change. Using array index as key causes bugs when items are reordered because React maps state to index, not to the actual item. Always use a stable, unique ID from your data." },
      { q: "What is the React component lifecycle?", a: "Mounting: component is created and inserted (useEffect with [] runs after). Updating: state or props change, component re-renders (useEffect with dependencies runs after). Unmounting: component is removed (useEffect cleanup function runs). The cleanup function in useEffect fully replaces componentWillUnmount from class components." },
      { q: "What is code splitting in React and how do you implement it?", a: "Code splitting breaks your JavaScript bundle into smaller chunks loaded on demand, reducing initial page load time. In React, use React.lazy(() => import('./Component')) combined with <Suspense fallback={<Spinner/>}>. Route-based splitting — lazy loading each page/route — gives the highest performance gain for most apps." },
      { q: "What are Higher-Order Components (HOC)?", a: "A HOC is a function that takes a component and returns a new enhanced component with additional props or behavior. Common patterns: withAuth (checks login before rendering), withLogger, or withTheme. HOCs add power but make component trees harder to trace; custom hooks are the modern preferred alternative for sharing logic between components." },
      { q: "What are custom hooks?", a: "Custom hooks are JavaScript functions whose name starts with 'use' that can call other hooks. They let you extract and share stateful logic between components without changing the component tree. Examples: useFetch (API calls with loading/error state), useLocalStorage, useDebounce — all reusable across any component." },
      { q: "What is the difference between Redux and Context API?", a: "Context API is simple and built-in, suitable for low-frequency global state like auth or theme. Redux provides a single predictable state tree, middleware (Thunk, Saga) for async logic, and excellent devtools for time-travel debugging. Use Context for simple needs; use Redux (or Zustand, Jotai) when state is complex, frequently updated, or shared across many unrelated components." },
      { q: "What is server-side rendering (SSR) in React?", a: "SSR renders React components on the server to full HTML, sends it to the browser for fast first paint, then hydrates it with JavaScript for interactivity. Benefits: improved SEO and faster Time to First Byte. Next.js is the standard tool — use getServerSideProps for per-request SSR or getStaticProps for build-time static generation (SSG)." },
      { q: "What is React Fiber?", a: "React Fiber is the reconciliation engine introduced in React 16 that reimplements the core algorithm to enable incremental rendering. It can pause, resume, and prioritise rendering work, enabling concurrent features like Suspense, lazy loading, and automatic batching. It's the foundation for React 18's concurrent mode." },
      { q: "What is React StrictMode?", a: "StrictMode is a development-only wrapper that intentionally double-invokes render functions and effects to surface side effect bugs early. It also warns about deprecated lifecycle methods and legacy API usage. It has zero performance cost in production. Wrap your root app in <React.StrictMode> to catch issues before deployment." },
      { q: "How do you optimise a React application's performance?", a: "Key strategies: use React.memo/useMemo/useCallback to prevent unnecessary re-renders, split code with React.lazy, virtualise long lists with react-window, avoid creating new objects/functions inline in JSX (breaks memoization), keep state as local as possible, use the React Profiler to identify bottlenecks, and ensure you're running the production build." },
      { q: "What is useLayoutEffect?", a: "useLayoutEffect fires synchronously after all DOM mutations but before the browser paints. This makes it ideal for reading DOM measurements (element dimensions, scroll position) and making synchronous DOM changes to prevent visual flicker. Use useEffect for everything else — useLayoutEffect blocks the browser paint and can hurt performance if misused." },
      { q: "What is the difference between React 17 and React 18?", a: "React 18 introduced automatic batching (state updates inside timeouts and promises are now batched too), the createRoot API, Suspense for data fetching, the useTransition hook for non-urgent state updates, and the useDeferredValue hook. Concurrent mode is now the default, making React smarter about scheduling renders to keep the UI responsive." },
    ],
  },

  javascript: {
    title: "JavaScript Interview Questions",
    slug: "javascript",
    icon: "🟨",
    metaTitle: "Top 50 JavaScript Interview Questions & Answers (2025)",
    metaDesc: "Master JavaScript interviews with 50 most-asked questions for 2025. Covers closures, event loop, promises, async/await, prototypes, ES6+, and more.",
    intro: "JavaScript is asked in every frontend, full-stack, and even backend role. These questions cover core concepts that interviewers at TCS, Infosys, Razorpay, CRED, and every FAANG company test in their JavaScript rounds.",
    related: ["react", "typescript", "nodejs"],
    questions: [
      { q: "What is the difference between var, let, and const?", a: "var is function-scoped, gets hoisted with a value of undefined, and can be re-declared. let and const are block-scoped (within {}), are hoisted but not initialised (temporal dead zone), and cannot be re-declared. const additionally prevents reassignment of the variable binding — though the object it points to can still be mutated." },
      { q: "What is hoisting in JavaScript?", a: "Hoisting is JavaScript's behaviour of moving variable and function declarations to the top of their scope during compilation, before code executes. Function declarations are fully hoisted (callable before they appear). var variables are hoisted and initialised to undefined. let and const are hoisted but remain uninitialised (accessing them before declaration throws a ReferenceError — the temporal dead zone)." },
      { q: "What is a closure?", a: "A closure is a function that retains access to its outer (enclosing) function's variables even after that outer function has finished executing. JavaScript creates closures every time a function is created. They're used for data privacy (module pattern), factory functions, memoization, and in callbacks/event handlers where you need to 'remember' surrounding context." },
      { q: "What is the JavaScript event loop?", a: "JavaScript is single-threaded. The event loop continuously checks: if the call stack is empty, it takes the first task from the callback queue (or microtask queue first) and pushes it onto the stack. Microtasks (Promise callbacks, queueMicrotask) run before macrotasks (setTimeout, setInterval). This is why a resolved Promise callback runs before a setTimeout(fn, 0) callback." },
      { q: "What is the difference between == and ===?", a: "== is the loose equality operator and performs type coercion before comparing — 0 == '0' is true. === is the strict equality operator and returns false if the types differ — 0 === '0' is false. Always prefer === to avoid subtle bugs from unexpected type coercions. The only common exception is null == undefined (both are true with ==)." },
      { q: "What is a Promise and how does it work?", a: "A Promise represents the eventual result of an asynchronous operation. It has three states: pending, fulfilled (resolved with a value), or rejected (with a reason). You chain .then() for the success case, .catch() for errors, and .finally() to run code regardless. Promise.all() runs multiple promises in parallel; Promise.race() resolves/rejects as soon as one settles." },
      { q: "What is async/await and how does it relate to Promises?", a: "async/await is syntactic sugar over Promises that lets you write asynchronous code that reads like synchronous code. An async function always returns a Promise. await pauses execution of the async function until the Promise resolves, then returns the resolved value. Use try/catch around await for error handling. It doesn't replace Promises — it's built on top of them." },
      { q: "What is the difference between null and undefined?", a: "undefined means a variable has been declared but not assigned a value (also the default return value of functions). null is an intentional assignment meaning 'no value' — you explicitly set something to null to indicate emptiness. typeof undefined === 'undefined' but typeof null === 'object' (a long-standing JS quirk). Use === null or === undefined to check for them specifically." },
      { q: "What is the prototype chain in JavaScript?", a: "Every JavaScript object has an internal [[Prototype]] link to another object (or null). When you access a property, JS looks at the object first, then walks up the prototype chain until it finds the property or reaches null. This is how inheritance works in JS — Array.prototype methods like .map() are available on all arrays through this chain." },
      { q: "What is the 'this' keyword in JavaScript?", a: "'this' refers to the object that is executing the current function. In a method, this is the object the method belongs to. In a regular function, this is the global object (or undefined in strict mode). Arrow functions don't have their own this — they inherit it from the enclosing scope. Call, apply, and bind explicitly set what this refers to." },
      { q: "What are arrow functions and how do they differ from regular functions?", a: "Arrow functions have a shorter syntax and don't bind their own this, arguments, super, or new.target — they inherit these from the surrounding lexical scope. They cannot be used as constructors (no new keyword). They are ideal for callbacks and methods that need to access the parent scope's this, which is why they're heavily used in React." },
      { q: "What is destructuring in JavaScript?", a: "Destructuring is a syntax that lets you unpack values from arrays or properties from objects into variables. const { name, age } = person; const [first, second] = array;. It supports default values, renaming (const { name: userName } = user), and nested destructuring. Widely used with function parameters, imports, and React props." },
      { q: "What is the spread operator and rest parameter?", a: "The spread operator (...) expands an iterable into individual elements — [...arr1, ...arr2] merges arrays; {...obj1, ...obj2} merges objects (shallow). The rest parameter (...args) collects all remaining function arguments into an array. They use the same ... syntax but in opposite directions: spread expands, rest collects." },
      { q: "What is event bubbling and event capturing?", a: "When an event fires on a nested element, it goes through two phases: capturing (top-down from document to the target) and bubbling (bottom-up from target back to document). Most event listeners use bubbling by default. Use event.stopPropagation() to prevent the event from traveling further. Event delegation uses bubbling to handle events from many children on a single parent listener." },
      { q: "What is the difference between call(), apply(), and bind()?", a: "All three set the value of this explicitly. call(thisArg, arg1, arg2) invokes the function immediately with arguments listed individually. apply(thisArg, [argsArray]) invokes immediately with arguments as an array. bind(thisArg, arg1) returns a new function with this and optionally some arguments pre-set, for later invocation." },
      { q: "What are JavaScript modules (ES Modules)?", a: "ES Modules (ESM) use export and import to share code between files. Named exports: export const foo = ... then import { foo } from './file'. Default exports: export default function then import anything from './file'. Modules are deferred and evaluated once. They're the standard in modern JS, replacing CommonJS (require/module.exports) used in older Node.js code." },
      { q: "What are generator functions?", a: "Generator functions (function*) can pause execution and resume later. They return a generator object. yield pauses the function and returns a value; the next call to .next() resumes from where it left off. They're useful for lazy evaluation, infinite sequences, and implementing custom iterables. async/await is conceptually built on generators." },
      { q: "What is the difference between map(), filter(), and reduce()?", a: "map() transforms every element and returns a new array of the same length. filter() returns a new array with only the elements that pass a predicate test. reduce() accumulates all elements into a single value (sum, object, nested array, etc.) using a reducer function and initial accumulator. All three are pure functions that don't mutate the original array." },
      { q: "What is a WeakMap and when would you use it?", a: "A WeakMap is like a Map but its keys must be objects and are held weakly — if no other reference to a key object exists, it can be garbage collected along with its entry in the WeakMap. This prevents memory leaks when associating private data with DOM nodes or objects whose lifetime you don't control. WeakMaps are not iterable." },
      { q: "What is debouncing and throttling?", a: "Debouncing delays function execution until after a pause in calls — e.g., wait 300ms after the user stops typing before searching. Throttling limits how often a function can run — e.g., fire at most once every 200ms during scroll. Debounce collapses rapid calls into one at the end; throttle fires at a regular interval during continuous events." },
      { q: "What is the difference between synchronous and asynchronous code?", a: "Synchronous code executes line by line, blocking further execution until each operation completes. Asynchronous code (setTimeout, fetch, Promises) allows the program to continue while waiting for an operation to finish, using callbacks/promises/async-await to handle the result when it arrives. JavaScript is single-threaded so async operations are handled by the browser's Web APIs and the event loop." },
      { q: "What are JavaScript design patterns? Name a few.", a: "Common patterns: Module (encapsulation via closures/IIFE), Singleton (one instance), Observer/Pub-Sub (event emitters), Factory (create objects without specifying class), Decorator (wrap objects to add behaviour). In React: Container/Presentational pattern, Compound Components, Render Props, and Higher-Order Components are widely used." },
      { q: "What is localStorage vs sessionStorage vs cookies?", a: "localStorage persists data with no expiry until explicitly cleared. sessionStorage persists data only for the browser tab's lifetime — cleared when the tab closes. Cookies are sent with every HTTP request to the server, have a configurable expiry, and can be restricted to HTTPS (Secure) or made inaccessible to JS (HttpOnly). Use cookies for auth tokens, localStorage for UI preferences." },
      { q: "What is a Symbol in JavaScript?", a: "Symbol is a primitive type introduced in ES6 that creates unique, immutable identifiers. Every Symbol() call returns a unique value — Symbol('x') !== Symbol('x'). They're used as unique object property keys to avoid naming collisions (especially in library code), and as well-known symbols (Symbol.iterator, Symbol.toPrimitive) to customise built-in behaviour." },
      { q: "What is optional chaining (?.) and nullish coalescing (??)?", a: "Optional chaining (?.) safely accesses nested properties, returning undefined instead of throwing if a value is null or undefined: user?.address?.city. Nullish coalescing (??) returns the right-hand side only when the left is null or undefined (not 0 or ''): const name = user.name ?? 'Anonymous'. Together they make defensive code far cleaner than nested if-checks." },
    ],
  },

  java: {
    title: "Java Interview Questions",
    slug: "java",
    icon: "☕",
    metaTitle: "Top 50 Java Interview Questions & Answers (2025)",
    metaDesc: "Prepare for Java interviews with 50 top questions on OOP, collections, multithreading, JVM, Spring Boot, and design patterns asked at TCS, Infosys, Wipro, and product companies.",
    intro: "Java is the backbone of enterprise software in India — asked in every TCS, Infosys, Wipro, Cognizant campus drive and product company interview. These questions cover everything from core OOP to Spring Boot, multithreading, and JVM internals.",
    related: ["sql", "system-design", "data-structures"],
    questions: [
      { q: "What are the four pillars of Object-Oriented Programming?", a: "Encapsulation (binding data and methods together, hiding internals via access modifiers), Inheritance (a child class acquires properties of a parent class), Polymorphism (one interface, many implementations — method overloading and overriding), and Abstraction (hiding complexity, exposing only essential features via abstract classes or interfaces)." },
      { q: "What is the difference between an abstract class and an interface?", a: "An abstract class can have implemented methods, constructors, instance variables, and can extend only one class. An interface (prior to Java 8) only had abstract methods; since Java 8 it can have default and static methods but no instance state. A class can implement multiple interfaces but extend only one class. Use interfaces for capability contracts, abstract classes for shared base implementations." },
      { q: "What is the difference between == and .equals() in Java?", a: "== compares object references (memory addresses) for objects, and values for primitives. .equals() compares the logical content of objects — it's overridden in String, Integer, and other classes to compare values. Always use .equals() to compare String content; == on String literals may appear to work due to the String pool but is unreliable for non-literal strings." },
      { q: "What is the Java Memory Model? Explain heap and stack.", a: "The stack stores method call frames, local primitives, and object references — each thread has its own stack. The heap stores all objects and class instances — shared across threads. The Metaspace (replaced PermGen in Java 8) stores class metadata. Understanding this distinction is key for diagnosing OutOfMemoryErrors and threading issues." },
      { q: "What is garbage collection in Java?", a: "Java's garbage collector automatically reclaims memory from objects that are no longer reachable (no live references). The JVM uses generational GC: most objects are young (created and quickly collected in Eden space), survivors move to older generations. Modern collectors include G1 (default since Java 9), ZGC, and Shenandoah — all designed for low-pause-time collection." },
      { q: "What is the difference between ArrayList and LinkedList?", a: "ArrayList is backed by a dynamic array — O(1) random access but O(n) insertion/deletion in the middle. LinkedList is a doubly-linked list — O(1) insertion/deletion at ends but O(n) random access. Use ArrayList for most use cases (better cache locality); use LinkedList only when frequent insertions/deletions at both ends outweigh the overhead of pointer traversal." },
      { q: "What is the difference between HashMap and Hashtable?", a: "HashMap is not synchronised (not thread-safe), allows one null key and multiple null values, and is faster. Hashtable is synchronised (thread-safe via method-level locking), allows no null keys or values, and is slower. In modern code, use ConcurrentHashMap for thread-safe maps — it uses segment-level locking for much better concurrent performance than Hashtable." },
      { q: "What is the difference between checked and unchecked exceptions?", a: "Checked exceptions (IOException, SQLException) are verified at compile time — you must either catch them or declare them with throws. Unchecked exceptions (RuntimeException subclasses like NullPointerException, ArrayIndexOutOfBoundsException) are not checked at compile time. Checked exceptions model recoverable conditions; unchecked model programming errors." },
      { q: "What is multithreading and how do you create a thread in Java?", a: "Multithreading allows concurrent execution of multiple threads within a process, sharing heap memory. You can create threads by: (1) extending Thread class and overriding run(), (2) implementing Runnable and passing it to Thread, or (3) implementing Callable with an ExecutorService for tasks that return values. The modern approach is using ExecutorService and thread pools rather than managing threads directly." },
      { q: "What is synchronisation in Java and why is it needed?", a: "Synchronisation prevents race conditions by ensuring only one thread at a time executes a critical section. Use the synchronized keyword on methods or blocks. volatile ensures visibility of variable changes across threads but doesn't ensure atomicity. Java's java.util.concurrent package provides higher-level tools: ReentrantLock, Semaphore, CountDownLatch, and atomic classes." },
      { q: "What are Java Streams? (Java 8+)", a: "Streams provide a functional-style API for processing sequences of elements. They support operations like filter(), map(), reduce(), sorted(), collect(), and forEach(). Streams are lazy — intermediate operations are only executed when a terminal operation is called. They support parallel processing via parallelStream() to leverage multi-core CPUs automatically." },
      { q: "What are lambda expressions in Java?", a: "Lambdas are anonymous functions introduced in Java 8 that implement a functional interface (an interface with exactly one abstract method). Syntax: (params) -> expression or (params) -> { body }. They eliminate boilerplate anonymous inner classes. Combined with Streams and method references, they enable a functional programming style in Java." },
      { q: "What is the Optional class in Java 8?", a: "Optional<T> is a container that may or may not hold a non-null value, designed to replace null returns and reduce NullPointerExceptions. Key methods: Optional.of(value), Optional.empty(), .isPresent(), .get(), .orElse(default), .orElseGet(supplier), .map(), and .filter(). It forces callers to explicitly handle the absent case rather than blindly dereferencing." },
      { q: "What is the difference between String, StringBuilder, and StringBuffer?", a: "String is immutable — every modification creates a new object, making repeated concatenation in loops O(n²). StringBuilder is mutable and not thread-safe — the right choice for string construction in single-threaded code. StringBuffer is mutable and thread-safe (synchronized methods) but slower. Always use StringBuilder in loops; use + for simple one-line concatenation (the compiler optimises it anyway)." },
      { q: "What is Spring Boot and why is it used?", a: "Spring Boot is an opinionated framework that auto-configures a Spring application based on the dependencies on the classpath, eliminating most boilerplate XML and Java configuration. It embeds Tomcat/Jetty, provides production-ready actuator endpoints, and lets you create standalone REST APIs with minimal setup. It's the dominant choice for building Java microservices and REST APIs in India." },
      { q: "What is Dependency Injection (DI) and Inversion of Control (IoC)?", a: "IoC is the principle of inverting control of object creation from the class to an external container. DI is how IoC is implemented — dependencies are injected into a class (via constructor, setter, or field) rather than the class creating them. Spring's IoC container manages bean lifecycle and wiring. DI makes code testable, modular, and loosely coupled." },
      { q: "What is the difference between @Component, @Service, @Repository, and @Controller?", a: "All four are Spring stereotype annotations that declare a class as a Spring-managed bean. @Component is generic. @Service annotates the service layer (business logic). @Repository annotates the data access layer and adds exception translation. @Controller/@RestController annotates web layer classes handling HTTP requests. They are semantically different but functionally equivalent for component scanning." },
      { q: "What is JPA and Hibernate?", a: "JPA (Java Persistence API) is a specification for ORM (Object-Relational Mapping) in Java. Hibernate is the most popular implementation of JPA. Hibernate maps Java objects to database tables, translates method calls into SQL, manages transactions, and provides a cache. Spring Data JPA further simplifies this with repository interfaces that generate queries from method names." },
      { q: "What is the SOLID principle?", a: "SOLID is five object-oriented design principles: Single Responsibility (a class should have one reason to change), Open/Closed (open for extension, closed for modification), Liskov Substitution (subclasses should be substitutable for their parent), Interface Segregation (don't force clients to implement unused methods), Dependency Inversion (depend on abstractions, not concretions). Following SOLID leads to maintainable, testable code." },
      { q: "What is the difference between final, finally, and finalize()?", a: "final is a keyword: applied to a variable (constant), method (cannot be overridden), or class (cannot be extended). finally is a block in try-catch that always executes, used for cleanup (closing resources — though try-with-resources is preferred). finalize() is a deprecated method called by the GC before an object is collected — avoid relying on it for resource cleanup." },
    ],
  },

  python: {
    title: "Python Interview Questions",
    slug: "python",
    icon: "🐍",
    metaTitle: "Top 50 Python Interview Questions & Answers (2025)",
    metaDesc: "Ace Python interviews with 50 top questions on decorators, generators, OOP, GIL, list comprehensions, Django, and data science libraries asked in 2025.",
    intro: "Python dominates data science, backend development, and automation roles in India. These questions are asked at startups, product companies, and in data engineering interviews at companies like Flipkart, PhonePe, Razorpay, and every data-heavy startup.",
    related: ["sql", "data-structures", "system-design"],
    questions: [
      { q: "What is Python and what makes it popular?", a: "Python is a high-level, interpreted, dynamically-typed language known for its readable syntax. Its popularity comes from: a vast standard library and ecosystem (NumPy, Pandas, Django, Flask), strong community, versatility across web development, data science, ML, automation, and scripting, and the fact that it reads almost like plain English, lowering the learning curve." },
      { q: "What are Python decorators?", a: "A decorator is a function that wraps another function to extend its behaviour without modifying it. The @decorator syntax is shorthand for func = decorator(func). Common uses: logging, authentication checks (@login_required in Django), caching (@functools.lru_cache), rate limiting, and timing function execution. Decorators are a powerful application of Python's first-class functions and closures." },
      { q: "What is the difference between a list, tuple, and set?", a: "Lists are ordered, mutable, and allow duplicates — use for sequences you'll modify. Tuples are ordered, immutable, and allow duplicates — use for fixed records, dict keys, or when you want to signal that data shouldn't change. Sets are unordered, mutable, and contain no duplicates — use for membership tests (O(1) lookup), deduplication, and set operations (union, intersection)." },
      { q: "What is a generator in Python?", a: "A generator is a function that uses yield instead of return to produce a sequence of values lazily — one at a time, on demand. Calling a generator function returns a generator object; values are computed only when iterated. This makes generators memory-efficient for large datasets. Generator expressions ((x*2 for x in range(n))) are like lazy list comprehensions." },
      { q: "What is the GIL (Global Interpreter Lock)?", a: "The GIL is a mutex in CPython that allows only one thread to execute Python bytecode at a time, even on multi-core systems. This means Python threads don't truly run in parallel for CPU-bound work. Workarounds: use multiprocessing (separate processes, each with their own GIL) for CPU-bound tasks, or use async/await (asyncio) for I/O-bound concurrency. NumPy operations release the GIL." },
      { q: "What are list comprehensions?", a: "List comprehensions provide a concise way to create lists: [expression for item in iterable if condition]. They are faster than equivalent for loops (optimised in CPython) and more readable. dict comprehensions ({k: v for k, v in items}), set comprehensions ({x for x in iterable}), and generator expressions ((x for x in iterable)) follow the same syntax." },
      { q: "What is the difference between *args and **kwargs?", a: "*args collects additional positional arguments into a tuple. **kwargs collects additional keyword arguments into a dictionary. They let functions accept variable numbers of arguments. The names args and kwargs are conventions — the * and ** are the actual syntax. Order in function signatures must be: positional, *args, keyword-only, **kwargs." },
      { q: "What are Python's data model (dunder/magic) methods?", a: "Dunder methods (__init__, __str__, __repr__, __len__, __getitem__, __eq__, __lt__, etc.) define how objects interact with Python's built-in operations. __init__ is the constructor. __str__ defines str(obj). __repr__ defines repr(obj) for debugging. __len__ enables len(obj). __getitem__ enables obj[key]. Implementing these makes your classes feel like native Python types." },
      { q: "What is the difference between deep copy and shallow copy?", a: "A shallow copy (copy.copy()) creates a new object but inserts references to the original's nested objects — mutating a nested list in the copy affects the original. A deep copy (copy.deepcopy()) recursively copies all nested objects, creating fully independent duplicates. For simple objects with no nesting, both behave the same." },
      { q: "What is a Python context manager and the 'with' statement?", a: "A context manager handles setup and teardown automatically. The with statement calls __enter__ on entry and __exit__ on exit (even if an exception occurs). The most common use is file handling: with open('file') as f — the file is guaranteed to close after the block. You can create custom context managers using the contextlib.contextmanager decorator." },
      { q: "What is the difference between is and ==?", a: "== compares values (calls __eq__). is compares identity — whether two variables point to the exact same object in memory. is is appropriate for comparing with None (use x is None, not x == None), True, and False. CPython caches small integers (-5 to 256) and interned strings, so is may return True for these unexpectedly — never rely on is for value comparison." },
      { q: "What is Python's MRO (Method Resolution Order)?", a: "MRO is the order Python uses to look up methods in a class hierarchy with multiple inheritance. Python uses the C3 linearization algorithm. You can inspect it with ClassName.__mro__ or ClassName.mro(). The super() function follows MRO, enabling cooperative multiple inheritance. Understanding MRO prevents the diamond problem where the same method exists in multiple parent classes." },
      { q: "What is the difference between a module and a package?", a: "A module is a single .py file that can be imported. A package is a directory with an __init__.py file (can be empty) that groups related modules. Packages can be nested. __init__.py runs when the package is first imported and can expose a simplified API. Modern Python (3.3+) also supports namespace packages without __init__.py." },
      { q: "What are lambda functions in Python?", a: "Lambda functions are anonymous, single-expression functions: lambda x, y: x + y. They're syntactically restricted to one expression and return it implicitly. Use them for short callbacks passed to map(), filter(), sorted(key=...), or any function expecting a callable. For anything more complex, a named function is more readable and testable." },
      { q: "How does Python handle memory management?", a: "Python uses reference counting as its primary memory management mechanism — an object is deallocated when its reference count drops to zero. A cyclic garbage collector handles reference cycles (e.g., a references b, b references a). The gc module exposes this collector. Python also uses memory pools (pymalloc) for small objects to reduce fragmentation and allocation overhead." },
      { q: "What is the difference between @staticmethod and @classmethod?", a: "@staticmethod defines a method that belongs to the class but receives neither the instance (self) nor the class (cls) as the first argument — it's essentially a namespaced plain function. @classmethod receives the class as the first argument (cls) and can access/modify class state. @classmethod is commonly used as alternative constructors (e.g., Date.from_string())." },
      { q: "What is virtualenv and why do you use it?", a: "Virtualenv (or venv in Python 3) creates isolated Python environments with their own installed packages, preventing dependency conflicts between projects. Each project gets its own copy of pip-installed libraries. Best practice: always create a virtual environment per project, commit requirements.txt (generated with pip freeze), and never install project dependencies globally." },
      { q: "What are Python's built-in data structures?", a: "Python's core data structures: list (ordered, mutable sequence), tuple (ordered, immutable), set (unordered, unique items), frozenset (immutable set), dict (key-value mapping), and collections module extras: deque (double-ended queue), Counter, defaultdict, OrderedDict, and namedtuple. Choosing the right structure — based on access patterns — is key to writing efficient Python." },
      { q: "What is asyncio and when would you use it?", a: "asyncio is Python's standard library for writing concurrent code using the async/await syntax and an event loop. It's ideal for I/O-bound operations (network calls, file reads, database queries) where threads spend most time waiting. async def defines a coroutine; await pauses it while waiting for another coroutine. Use asyncio + httpx/aiohttp for high-throughput async HTTP clients and servers." },
      { q: "What is the difference between Django and Flask?", a: "Django is a batteries-included framework with a built-in ORM, admin panel, authentication, URL routing, forms, and templates — best for large applications with complex data models. Flask is a minimal microframework giving you just routing and request handling — you pick your own ORM, auth library, and templates. Django provides structure; Flask provides flexibility." },
    ],
  },

  nodejs: {
    title: "Node.js Interview Questions",
    slug: "nodejs",
    icon: "🟢",
    metaTitle: "Top 50 Node.js Interview Questions & Answers (2025)",
    metaDesc: "Prepare for Node.js interviews with 50 top questions on event loop, streams, modules, Express, clustering, async patterns, and microservices asked in 2025.",
    intro: "Node.js powers the backend of most modern Indian startups — Razorpay, CRED, Meesho, and dozens of others. These questions cover the event loop, async patterns, Express, streams, and system design concepts you'll face in backend JavaScript roles.",
    related: ["javascript", "react", "sql"],
    questions: [
      { q: "What is Node.js and what makes it non-blocking?", a: "Node.js is a JavaScript runtime built on Chrome's V8 engine that executes JavaScript outside the browser. It's non-blocking because it uses an event-driven, single-threaded architecture with an event loop that delegates I/O operations to the operating system's asynchronous APIs (via libuv). While I/O waits, the event loop handles other requests, making Node excellent for I/O-bound concurrent workloads." },
      { q: "How does the Node.js event loop work?", a: "The event loop processes tasks in phases: timers (setTimeout/setInterval callbacks), pending callbacks (I/O errors), idle/prepare, poll (wait for new I/O, execute callbacks), check (setImmediate callbacks), close callbacks. Microtasks (process.nextTick and Promises) run between phases. process.nextTick runs before any I/O — even before Promises." },
      { q: "What is the difference between process.nextTick() and setImmediate()?", a: "process.nextTick() queues a callback to run after the current operation completes but before the event loop continues to the next phase — it has the highest priority among async callbacks. setImmediate() runs in the check phase, after the poll (I/O) phase. In practice: nextTick runs before Promises; setImmediate runs after I/O callbacks in the current loop iteration." },
      { q: "What is the CommonJS module system in Node.js?", a: "CommonJS is Node.js's original module system using require() to import and module.exports or exports to export. Modules are cached after first load. Node.js also supports ES Modules (import/export) in .mjs files or when type: 'module' is set in package.json. The two systems are not interoperable without adapters — mixing them in the same project requires care." },
      { q: "What are streams in Node.js?", a: "Streams are abstract interfaces for working with streaming data, enabling processing of large files or network data without loading everything into memory. Four types: Readable (fs.createReadStream), Writable (fs.createWriteStream), Duplex (readable + writable, like a TCP socket), and Transform (modify data in transit, like zlib.createGzip). Streams use events: data, end, error, finish." },
      { q: "What is the difference between spawn(), exec(), and fork() in child_process?", a: "spawn() launches a new process and streams stdin/stdout — ideal for long-running commands producing large output. exec() runs a command in a shell and buffers the entire output — suitable for small, quick commands. fork() is a special spawn for Node.js modules — it creates a new Node.js process with a built-in IPC (message-passing) channel between parent and child." },
      { q: "What is clustering in Node.js and why do you use it?", a: "Node.js is single-threaded, so it can't natively use multiple CPU cores. The cluster module lets you spawn multiple child processes (workers) that all share the same server port. The master process distributes incoming connections to workers. A common pattern: spawn os.cpus().length workers. PM2 manages clustering in production with zero-downtime restarts." },
      { q: "What is the EventEmitter in Node.js?", a: "EventEmitter is a core Node.js class for implementing the observer pattern. Objects extend EventEmitter to emit named events (emitter.emit('event', data)) and register listeners (emitter.on('event', callback)). Most Node.js core APIs (streams, http, etc.) extend EventEmitter. Common pitfalls: forgetting to remove listeners (memory leaks) and unhandled 'error' events crashing the process." },
      { q: "What is middleware in Express.js?", a: "Middleware are functions in Express's request-response cycle with access to (req, res, next). They can execute code, modify req/res, end the request, or call next() to pass control to the next middleware. Used for: logging, auth checks, request parsing (express.json()), CORS headers, error handling. Error-handling middleware has four parameters: (err, req, res, next)." },
      { q: "How do you handle errors in Node.js?", a: "Synchronous: try-catch. Async (callbacks): check the first argument (err-first convention). Promises: .catch() or try-catch in async/await. Add a global process.on('unhandledRejection') and process.on('uncaughtException') as last-resort handlers. In Express, use a 4-parameter error-handling middleware. Always log errors with context and avoid silently swallowing them." },
      { q: "What is the difference between SQL and NoSQL databases?", a: "SQL databases (PostgreSQL, MySQL) store data in structured tables with predefined schemas, enforce ACID transactions, and use JOIN queries for relational data. NoSQL databases (MongoDB, Redis, DynamoDB) are schema-flexible, optimise for specific access patterns, and typically sacrifice full ACID guarantees for horizontal scalability. Use SQL when data relationships and consistency matter; NoSQL when flexibility and scale are the priority." },
      { q: "What is CORS and how do you handle it in Node.js?", a: "CORS (Cross-Origin Resource Sharing) is a browser security mechanism that blocks frontend code from making requests to a different origin (domain/port) than the one that served the page. To allow it server-side, set Access-Control-Allow-Origin response headers. In Express, the cors package handles this: app.use(cors({ origin: 'https://yourfrontend.com' }))." },
      { q: "What is JWT and how is it used for authentication?", a: "A JWT (JSON Web Token) is a compact, URL-safe token with three base64-encoded parts: header (algorithm), payload (claims like userId, role, expiry), and a signature. The server signs the token on login; the client stores it (httpOnly cookie or memory) and sends it with requests. The server verifies the signature without a database lookup, making JWTs stateless and scalable." },
      { q: "What is connection pooling in databases?", a: "Creating a new database connection for every request is expensive (TCP handshake, authentication, resource allocation). A connection pool maintains a set of pre-established connections that are reused across requests. Libraries like pg-pool (PostgreSQL) or Mongoose manage pools automatically. Set pool size based on your database's max connections and application concurrency." },
      { q: "What is the purpose of package.json?", a: "package.json is the manifest for a Node.js project. It records the project's name, version, entry point, scripts (npm run dev, npm test), dependencies (required at runtime), devDependencies (only for development/testing), and engines (Node.js version range). package-lock.json (or yarn.lock) locks exact dependency versions for reproducible installs across environments." },
      { q: "How do you prevent a Node.js server from crashing?", a: "Never let uncaught exceptions crash production silently: handle process.on('uncaughtException') and process.on('unhandledRejection'). Use a process manager like PM2 with automatic restarts. Add proper error handling in all async paths. Use a load balancer with health checks to route traffic away from crashed instances. Implement circuit breakers for calls to external services that may fail." },
      { q: "What is rate limiting and why is it important?", a: "Rate limiting restricts how many requests a client can make in a time window, protecting your API from abuse, brute-force attacks, and accidental overload. Implement with express-rate-limit (in-process, not for multi-instance), or Redis (shared state across instances) with sliding window or token bucket algorithms. Always rate limit auth endpoints, AI calls, and any expensive operation." },
      { q: "What is the difference between authentication and authorisation?", a: "Authentication verifies who you are (login — checking credentials to establish identity). Authorisation determines what you're allowed to do (checking permissions after identity is established). Example: authentication confirms you are a logged-in user; authorisation checks whether that user has admin rights. Both are necessary; failing to distinguish them is a common security mistake." },
      { q: "What are environment variables and why do you use them?", a: "Environment variables externalise configuration — API keys, database URLs, secrets — from source code. This prevents secrets from being committed to version control and allows the same codebase to run with different configs (dev, staging, production). In Node.js, access them via process.env.MY_VAR. Use a .env file (read by dotenv in dev) and never commit it to git." },
      { q: "What is REST and what are its principles?", a: "REST (Representational State Transfer) is an architectural style for APIs. Its principles: stateless (each request contains all info needed), client-server separation, uniform interface (standard HTTP methods: GET, POST, PUT, PATCH, DELETE), resource-based URLs (/users/123 not /getUser), and cacheable responses. A RESTful API uses status codes correctly (200 OK, 201 Created, 400 Bad Request, 401 Unauthorized, 404 Not Found, 500 Server Error)." },
    ],
  },

  sql: {
    title: "SQL Interview Questions",
    slug: "sql",
    icon: "🗄️",
    metaTitle: "Top 50 SQL Interview Questions & Answers (2025)",
    metaDesc: "Crack SQL interviews with 50 top questions on joins, indexes, normalization, transactions, window functions, and query optimization for 2025.",
    intro: "SQL is tested in every backend, data engineering, data analyst, and SDE interview. Whether you're joining TCS as a fresher or interviewing for a data role at Flipkart or Swiggy, these are the queries and concepts you will be asked.",
    related: ["java", "python", "system-design"],
    questions: [
      { q: "What is the difference between INNER JOIN, LEFT JOIN, RIGHT JOIN, and FULL OUTER JOIN?", a: "INNER JOIN returns rows that have matching values in both tables. LEFT JOIN returns all rows from the left table and matched rows from the right (NULL for no match). RIGHT JOIN is the mirror — all rows from the right. FULL OUTER JOIN returns all rows from both, with NULLs where there's no match. LEFT JOIN is the most commonly used; INNER JOIN is the default." },
      { q: "What is the difference between WHERE and HAVING?", a: "WHERE filters rows before grouping (before GROUP BY is applied) and cannot use aggregate functions. HAVING filters groups after GROUP BY and can use aggregate functions (HAVING COUNT(*) > 5). Rule of thumb: WHERE filters individual rows; HAVING filters aggregated groups. You can use both in the same query." },
      { q: "What are indexes and how do they improve query performance?", a: "An index is a data structure (typically a B-tree) that allows the database to find rows without scanning the entire table. Like a book index, it maps column values to row locations. Indexes dramatically speed up SELECT queries with WHERE, JOIN, and ORDER BY on indexed columns but slow down INSERT/UPDATE/DELETE (the index must be maintained). Create indexes on columns used in WHERE and JOIN conditions." },
      { q: "What is database normalisation? Explain 1NF, 2NF, 3NF.", a: "Normalisation reduces data redundancy and improves integrity. 1NF: each column has atomic (indivisible) values, no repeating groups. 2NF: 1NF + every non-key attribute is fully functionally dependent on the entire primary key (no partial dependency — matters for composite keys). 3NF: 2NF + no transitive dependencies (non-key attribute doesn't depend on another non-key attribute)." },
      { q: "What is a PRIMARY KEY vs UNIQUE KEY?", a: "A PRIMARY KEY uniquely identifies each row, cannot be NULL, and there can only be one per table. A UNIQUE KEY also enforces uniqueness but allows one NULL and there can be multiple per table. Both create an index automatically. Use PRIMARY KEY for the entity identifier; use UNIQUE constraints for other columns that must be unique (email, username)." },
      { q: "What is a transaction? What are ACID properties?", a: "A transaction is a sequence of operations executed as a single unit. ACID properties ensure reliability: Atomicity (all operations succeed or all are rolled back), Consistency (the database moves from one valid state to another), Isolation (concurrent transactions appear sequential), Durability (committed data survives system failures). Transactions are critical for financial operations." },
      { q: "What is the difference between DELETE, TRUNCATE, and DROP?", a: "DELETE removes specific rows based on a WHERE clause — it's DML, logged, and can be rolled back. TRUNCATE removes all rows from a table instantly — it's DDL, minimally logged, cannot be used with WHERE, and in many databases cannot be rolled back within a transaction. DROP removes the entire table structure and data permanently." },
      { q: "What are window functions in SQL?", a: "Window functions perform calculations across a set of rows related to the current row (a 'window') without collapsing them into groups like GROUP BY. Common functions: ROW_NUMBER(), RANK(), DENSE_RANK() for ranking; LAG(), LEAD() for accessing previous/next rows; SUM(), AVG() with OVER (PARTITION BY col ORDER BY col). Essential for analytics queries." },
      { q: "What is a subquery vs a CTE?", a: "A subquery is a query nested inside another query (in WHERE, FROM, or SELECT clause). A CTE (Common Table Expression) uses the WITH keyword to define a named, temporary result set you can reference multiple times in the main query. CTEs are more readable for complex queries, can be recursive, and some databases optimise them as materialised results." },
      { q: "What is the difference between UNION and UNION ALL?", a: "UNION combines result sets of two queries and removes duplicate rows (requires sorting/hashing). UNION ALL combines result sets and keeps all duplicates — it's faster because no deduplication is needed. Use UNION when duplicates are a concern; use UNION ALL for performance when you know results are distinct or duplicates are acceptable." },
      { q: "How do you find duplicate records in a table?", a: "Use GROUP BY and HAVING: SELECT email, COUNT(*) as cnt FROM users GROUP BY email HAVING COUNT(*) > 1. To see the full duplicate rows, join this back to the original table. To delete duplicates keeping only one, use a CTE with ROW_NUMBER() PARTITION BY the duplicate-identifying columns and delete rows where row_number > 1." },
      { q: "What is a stored procedure vs a function?", a: "A stored procedure is a precompiled block of SQL that can perform actions, has no mandatory return value, and can use OUT parameters and transactions. A function must return a value, can be used in SELECT statements and expressions, and cannot have side effects (in most databases). Use stored procedures for multi-step operations; functions for reusable computations in queries." },
      { q: "What is query optimization? How do you tune a slow query?", a: "Steps: (1) Use EXPLAIN/EXPLAIN ANALYZE to view the query plan and find full table scans. (2) Add indexes on WHERE and JOIN columns. (3) Avoid SELECT * — select only needed columns. (4) Avoid functions on indexed columns in WHERE (WHERE YEAR(created_at) = 2024 prevents index use; use WHERE created_at BETWEEN ... instead). (5) Check for N+1 query patterns. (6) Consider denormalisation or caching for frequently-run expensive reads." },
      { q: "What are the different types of SQL commands (DDL, DML, DCL, TCL)?", a: "DDL (Data Definition Language): CREATE, ALTER, DROP, TRUNCATE — defines schema structure. DML (Data Manipulation Language): SELECT, INSERT, UPDATE, DELETE — manipulates data. DCL (Data Control Language): GRANT, REVOKE — manages permissions. TCL (Transaction Control Language): COMMIT, ROLLBACK, SAVEPOINT — manages transactions." },
      { q: "What is the N+1 query problem?", a: "The N+1 problem occurs when you fetch N records and then execute one additional query per record to get related data — resulting in N+1 total queries. Example: fetching 100 users then querying each user's orders separately = 101 queries. Fix by using JOIN or a batch query (SELECT * FROM orders WHERE user_id IN (1,2,...,100)) to retrieve all related data in one or two queries." },
      { q: "What is database sharding?", a: "Sharding is horizontal partitioning of a database across multiple servers (shards), each holding a subset of the data. Shard key determines which shard a record belongs to (e.g., user_id % num_shards). Benefits: scales writes and storage beyond a single server. Challenges: cross-shard queries are expensive, resharding is hard, and transactions across shards are complex." },
      { q: "What is a foreign key and what does ON DELETE CASCADE do?", a: "A foreign key is a column (or group of columns) in a table that references the primary key of another table, enforcing referential integrity. ON DELETE CASCADE means when the referenced parent row is deleted, all child rows with that foreign key are automatically deleted too. Other options: SET NULL (set foreign key to NULL), RESTRICT (prevent deletion if children exist)." },
      { q: "Write a SQL query to find the second highest salary.", a: "Method 1 (subquery): SELECT MAX(salary) FROM employees WHERE salary < (SELECT MAX(salary) FROM employees). Method 2 (LIMIT/OFFSET): SELECT DISTINCT salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET 1. Method 3 (window function): SELECT salary FROM (SELECT salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS rnk FROM employees) t WHERE rnk = 2." },
      { q: "What is the difference between a clustered and a non-clustered index?", a: "A clustered index determines the physical order of rows in the table — the table's data is stored in the order of the clustered index. There can be only one per table (usually the primary key). A non-clustered index is a separate structure that stores the indexed columns with pointers back to the actual row. A table can have many non-clustered indexes. Clustered index lookups are faster for range scans." },
      { q: "What is an execution plan and why is it useful?", a: "An execution plan (shown by EXPLAIN in PostgreSQL/MySQL) describes the steps the query optimizer chose to execute a SQL statement — join methods (hash join, nested loop), index usage, estimated row counts, and cost. Reading it helps you understand why a query is slow: full table scans (Seq Scan), missing indexes, bad row estimates, or inefficient join order. It's the first tool to reach for when tuning queries." },
    ],
  },

  typescript: {
    title: "TypeScript Interview Questions",
    slug: "typescript",
    icon: "🔷",
    metaTitle: "Top 40 TypeScript Interview Questions & Answers (2025)",
    metaDesc: "Master TypeScript interviews with 40 top questions on types vs interfaces, generics, utility types, decorators, and strict mode for 2025.",
    intro: "TypeScript is now expected in most frontend and full-stack roles at Indian product companies. These questions cover everything from basic type annotations to advanced generics and utility types asked at Razorpay, CRED, Zepto, and modern startups.",
    related: ["javascript", "react", "nodejs"],
    questions: [
      { q: "What is TypeScript and why do you use it?", a: "TypeScript is a statically-typed superset of JavaScript developed by Microsoft that compiles to plain JavaScript. It adds optional static typing, interfaces, enums, generics, and better tooling (autocomplete, error detection at compile time). Benefits: catches type errors before runtime, improves code documentation through types, and enables safer refactoring. It's the default choice for new React and Node.js projects." },
      { q: "What is the difference between type and interface?", a: "Both define the shape of an object. Key differences: interfaces can be extended with extends and merged via declaration merging (defining the same interface twice merges them). Type aliases can represent primitives, unions, tuples, and mapped types — more expressive. For object shapes, either works; prefer interface for public APIs (declaration merging is useful) and type for unions, intersections, and complex type expressions." },
      { q: "What are generics in TypeScript?", a: "Generics allow you to write reusable code that works with multiple types while maintaining type safety. function identity<T>(arg: T): T { return arg; } — the T is a type variable that's determined at call time. Generics are used throughout TypeScript's standard library: Array<T>, Promise<T>, Map<K,V>. Constraints (<T extends object>) narrow what types are accepted." },
      { q: "What is the difference between any, unknown, and never?", a: "any disables type checking entirely — avoid it as it defeats TypeScript's purpose. unknown is type-safe any — you must narrow the type with typeof or instanceof before using it. never represents a value that never occurs — the return type of functions that always throw, infinite loops, or branches that are logically impossible. It's also the bottom type (no value is assignable to never)." },
      { q: "What are union types and intersection types?", a: "A union type (A | B) means a value can be of type A or B. A function accepting string | number handles both. An intersection type (A & B) combines multiple types — a value must satisfy all of them. Intersections are used to compose types: type Admin = User & { adminLevel: number }. Use unions for alternatives; use intersections for combinations." },
      { q: "What are TypeScript utility types?", a: "Built-in generic types that transform existing types: Partial<T> makes all properties optional; Required<T> makes all required; Readonly<T> makes all read-only; Pick<T, K> selects a subset of properties; Omit<T, K> excludes properties; Record<K, V> creates an object type; Exclude<T, U> removes union members; ReturnType<T> extracts a function's return type. Mastering these eliminates repetitive type definitions." },
      { q: "What is type narrowing?", a: "Type narrowing is TypeScript's ability to refine a type to a more specific type within a conditional block. It uses type guards: typeof (typeof x === 'string'), instanceof (x instanceof Date), truthiness checks, equality checks, and discriminated unions. Custom type predicates (function isString(x: any): x is string) let you create reusable narrowing functions." },
      { q: "What is a discriminated union?", a: "A discriminated union is a union of types that share a common literal property (the discriminant) allowing TypeScript to narrow to the correct type in a switch/if. Example: type Shape = { kind: 'circle'; radius: number } | { kind: 'rect'; width: number }. Switching on shape.kind lets TypeScript know the exact shape in each branch. It's the idiomatic TypeScript pattern for modelling variant types." },
      { q: "What are TypeScript decorators?", a: "Decorators are an experimental feature (enabled with experimentalDecorators) that add metadata and modify classes, methods, properties, or parameters using @syntax. They're central to Angular and used in NestJS for @Injectable(), @Controller(), @Get(). Under the hood they're functions that receive the decorated target as an argument. TypeScript 5.0 introduced the TC39 Stage 3 decorators proposal as the new standard." },
      { q: "What is the difference between interface and abstract class in TypeScript?", a: "An interface defines a contract (shape) with no implementation — multiple interfaces can be implemented. An abstract class can have implemented methods, constructors, and properties, but cannot be instantiated directly. A class can only extend one abstract class. Use interfaces for pure contracts; use abstract classes when you want to share partial implementations while enforcing a contract." },
      { q: "What is declaration merging in TypeScript?", a: "Declaration merging allows TypeScript to merge multiple declarations with the same name into a single definition. Most commonly used to extend third-party interfaces — you can add properties to the Express Request interface without modifying the library source. Also used to combine namespace declarations. Types cannot be merged; only interfaces and namespaces support this." },
      { q: "What are mapped types in TypeScript?", a: "Mapped types transform existing types by iterating over their keys. type Optional<T> = { [K in keyof T]?: T[K] } creates a type with all properties of T made optional. You can add (readonly) or remove (-readonly, -?) modifiers. Mapped types are the basis for Partial, Required, Readonly, and Record. Combined with conditional types, they enable very expressive type-level programming." },
      { q: "What are conditional types?", a: "Conditional types have the form T extends U ? X : Y — if T is assignable to U, the type is X; otherwise Y. Used in utility types: NonNullable<T> is T extends null | undefined ? never : T. The infer keyword within conditional types extracts type information: ReturnType<T> uses T extends (...args: any) => infer R ? R : never to extract the return type of a function type." },
      { q: "What is strict mode in TypeScript?", a: "TypeScript's strict flag in tsconfig.json enables a group of strict type-checking options: strictNullChecks (null and undefined are not assignable to other types), noImplicitAny (error on implicit any type), strictFunctionTypes, strictBindCallApply, and others. Always enable strict: true in new projects — it catches the most common bugs and is now the community standard." },
      { q: "What is the difference between extends and implements in TypeScript?", a: "extends is used for inheritance: a class that extends another gets all its methods and properties (and must call super() in the constructor). implements is used to satisfy an interface contract: the class must provide all the interface's required properties and methods but gets no implementation from the interface. A class can implement multiple interfaces but extend only one class." },
    ],
  },

  "system-design": {
    title: "System Design Interview Questions",
    slug: "system-design",
    icon: "🏗️",
    metaTitle: "Top 40 System Design Interview Questions & Answers (2025)",
    metaDesc: "Crack system design interviews with 40 top questions on load balancing, caching, databases, microservices, CAP theorem, and scalability for senior roles in 2025.",
    intro: "System design interviews are the deciding round for SDE-2 and above roles at every product company. These questions on load balancing, caching, databases, and microservices are asked at Flipkart, Amazon, Google, Razorpay, and CRED for senior engineering positions.",
    related: ["sql", "java", "nodejs"],
    questions: [
      { q: "How would you design a URL shortener like bit.ly?", a: "Key components: URL generation (base62 encode a unique ID or hash the URL), storage (a simple key-value store like Redis for hot URLs + MySQL for persistence), redirection (HTTP 301 for permanent, 302 for tracking), analytics (count clicks async via a message queue). Scale: use consistent hashing to distribute ID generation, CDN for edge redirects, DB read replicas. Handle collision in hashing with random suffix or retry." },
      { q: "What is the difference between horizontal and vertical scaling?", a: "Vertical scaling (scaling up) means adding more resources to a single server (more CPU, RAM). It's simple but has limits and is a single point of failure. Horizontal scaling (scaling out) means adding more servers to distribute load. It requires a load balancer, stateless services (or shared session storage), and distributed data. Most modern architectures prefer horizontal scaling for high availability and cost efficiency." },
      { q: "What is a load balancer and what algorithms does it use?", a: "A load balancer distributes incoming traffic across multiple backend servers for high availability and scalability. Algorithms: Round Robin (cyclic distribution), Weighted Round Robin (based on server capacity), Least Connections (route to server with fewest active connections), IP Hash (consistent routing for a client to the same server — useful for session affinity), and Random. L4 (TCP) vs L7 (HTTP-aware) load balancers differ in what they can inspect." },
      { q: "What is caching and what are common caching strategies?", a: "Caching stores frequently-accessed data in fast storage (Redis, Memcached) to reduce database load and latency. Strategies: Cache-aside (app checks cache, falls back to DB, writes to cache — most common), Write-through (write to cache and DB synchronously), Write-behind (write to cache, async to DB), Read-through (cache sits in front of DB). Cache invalidation (when to evict stale data) is the hardest problem." },
      { q: "What is the CAP theorem?", a: "CAP theorem states that a distributed system can guarantee at most two of three properties: Consistency (every read returns the most recent write), Availability (every request gets a response, even if not the latest data), and Partition Tolerance (system continues despite network partitions). Since partitions are inevitable in distributed systems, you choose between CP (consistent but may be unavailable during partitions — e.g., HBase) and AP (always available but may return stale data — e.g., DynamoDB, Cassandra)." },
      { q: "What is the difference between SQL and NoSQL databases and when do you choose each?", a: "SQL (PostgreSQL, MySQL) enforces a schema, supports complex joins and ACID transactions — ideal when data relationships are complex and consistency is critical (banking, e-commerce orders). NoSQL: Document stores (MongoDB) for flexible schemas; Key-value (Redis) for caching and sessions; Wide-column (Cassandra) for write-heavy time-series at massive scale; Graph (Neo4j) for relationship-heavy queries. Most large systems use both (polyglot persistence)." },
      { q: "What is database replication and sharding?", a: "Replication copies data to multiple servers: a primary handles writes; read replicas handle reads (eventual consistency). This improves read throughput and availability. Sharding (horizontal partitioning) splits data across multiple databases based on a shard key (user_id % n). Sharding improves write throughput and storage but complicates cross-shard queries and transactions. Replication is for availability; sharding is for scale." },
      { q: "What is a message queue and when would you use one?", a: "A message queue (Kafka, RabbitMQ, SQS) decouples producers from consumers, enabling async processing. Use cases: smooth traffic spikes by buffering requests, background jobs (email sending, report generation), event-driven microservices communication, guaranteed delivery with retries. Kafka specifically is used for high-throughput event streaming and maintaining an ordered log of events." },
      { q: "What is a CDN and how does it work?", a: "A CDN (Content Delivery Network) is a globally distributed network of edge servers that cache static assets (images, JS, CSS) close to users, reducing latency. When a user requests a file, the CDN serves it from the nearest edge node. On a cache miss, the edge fetches from the origin server and caches the response. CDNs also absorb DDoS traffic and offload origin server bandwidth." },
      { q: "What is microservices architecture vs monolith?", a: "A monolith is a single deployable unit containing all functionality — simpler to develop, test, and debug but hard to scale and deploy individual parts. Microservices decompose the system into small, independently deployable services each owning its data. Benefits: independent scaling, independent deployment, technology flexibility. Challenges: distributed system complexity (network calls, distributed transactions, service discovery, observability)." },
      { q: "How do microservices communicate?", a: "Synchronous: REST over HTTP (simple, widely understood) or gRPC (binary, faster, typed contracts via Protocol Buffers — preferred for internal service-to-service). Asynchronous: message brokers (Kafka, RabbitMQ) for fire-and-forget or event-driven patterns. Async decouples services and improves resilience. Use sync for low-latency client-facing requests; async for background processing, high-throughput, and when strong decoupling matters." },
      { q: "What is a rate limiter and how would you design one?", a: "A rate limiter restricts the number of requests a client can make in a time window. Algorithms: Fixed Window (simple but allows bursts at window boundaries), Sliding Window Log (accurate but memory-intensive), Token Bucket (tokens accumulate at rate r, each request consumes one — allows short bursts), Leaky Bucket (requests queue and drain at a fixed rate). For distributed systems, store state in Redis with atomic Lua scripts or the INCR+EXPIRE pattern." },
      { q: "What is consistent hashing?", a: "Consistent hashing maps both data and nodes to a virtual ring (0 to 2^32). A key is assigned to the nearest node clockwise on the ring. When a node is added or removed, only the keys on that node's segment are redistributed (1/N keys on average), unlike naive modulo hashing where almost all keys move. Used in distributed caches and databases (Cassandra, DynamoDB) to minimise data movement during scaling." },
      { q: "How would you design a notification system?", a: "Components: notification service that receives events, a fan-out service to determine recipients, channel handlers (push, email, SMS, in-app), a message queue between them for async delivery, a user preferences service to filter channels, and a dedupe layer. At scale, Kafka fans out events to per-channel workers. Store notification history in a DB for in-app retrieval. Handle retries with exponential backoff and dead-letter queues." },
      { q: "What is an API gateway?", a: "An API gateway is a single entry point for all client requests to backend services. It handles cross-cutting concerns: authentication/authorisation, rate limiting, SSL termination, request routing, protocol translation, response caching, and logging. Examples: AWS API Gateway, Kong, NGINX. It simplifies clients (one endpoint instead of many) and moves infrastructure concerns out of individual services." },
      { q: "What is eventual consistency vs strong consistency?", a: "Strong consistency guarantees that after a write completes, all subsequent reads will return the updated value — requires coordination across replicas and reduces availability. Eventual consistency guarantees that given enough time without new updates, all replicas will converge to the same value — higher availability but reads may return stale data. Systems like DynamoDB and Cassandra offer tunable consistency levels between the two extremes." },
    ],
  },

  "data-structures": {
    title: "Data Structures & Algorithms Interview Questions",
    slug: "data-structures",
    icon: "🔢",
    metaTitle: "Top 50 DSA Interview Questions & Answers (2025)",
    metaDesc: "Crack DSA interviews with 50 top questions on arrays, linked lists, trees, graphs, sorting, dynamic programming, and time complexity for 2025.",
    intro: "DSA rounds are mandatory at every product company and FAANG interview in India. From Flipkart to Amazon to CRED, these are the most commonly asked algorithmic questions — along with the approach, time complexity, and key patterns you need to know.",
    related: ["java", "python", "system-design"],
    questions: [
      { q: "What is Big-O notation? What does O(n log n) mean?", a: "Big-O notation describes the worst-case time (or space) complexity of an algorithm as input size n grows. O(1) is constant, O(log n) is logarithmic (halving search space each step — binary search), O(n) is linear, O(n log n) is linearithmic (efficient sorting like merge sort), O(n²) is quadratic (nested loops), O(2^n) is exponential (recursive brute force). Focus on eliminating nested loops and repeated work." },
      { q: "What is the difference between an array and a linked list?", a: "Arrays store elements in contiguous memory — O(1) random access by index, O(n) insertion/deletion in middle (shifting required). Linked lists store elements in nodes with next pointers — O(n) access by index, O(1) insertion/deletion if you have the node pointer (no shifting). Arrays are better for cache performance and random access; linked lists for frequent insertions/deletions at known positions." },
      { q: "What is a stack and what is it used for?", a: "A stack is a LIFO (Last In, First Out) data structure with push, pop, and peek operations, all O(1). Used for: function call stack, undo operations, expression evaluation (infix to postfix), validating brackets (classic problem), DFS traversal (can replace recursion), and backtracking algorithms. Implement with a dynamic array or linked list." },
      { q: "What is a queue and what is it used for?", a: "A queue is a FIFO (First In, First Out) data structure with enqueue and dequeue operations, O(1) each. Used for: BFS graph traversal, level-order tree traversal, task scheduling, printer/CPU job queues, and sliding window problems (deque). A deque (double-ended queue) supports O(1) operations at both ends and is useful for monotonic queue patterns." },
      { q: "What is a binary search tree (BST)?", a: "A BST is a binary tree where every node's left subtree contains only nodes with values less than the node, and the right subtree only greater values. Search, insert, and delete are O(h) where h is height. A balanced BST (AVL, Red-Black Tree) guarantees O(log n) height. In-order traversal of a BST gives sorted output — a key property for many problems." },
      { q: "What is a hash map and how does it handle collisions?", a: "A hash map maps keys to values using a hash function to compute an array index. Hash collisions (two keys map to the same index) are handled by chaining (each bucket stores a linked list of entries — Java's HashMap) or open addressing (probe for the next empty slot — linear or quadratic probing). Average O(1) for get/put; O(n) worst case with many collisions." },
      { q: "What is BFS vs DFS and when do you use each?", a: "BFS (Breadth-First Search) explores level by level using a queue — finds the shortest path in unweighted graphs, level-order tree traversal, checking connectivity. DFS (Depth-First Search) explores as deep as possible using a stack/recursion — detects cycles, topological sort, finding connected components, maze-solving, and problems requiring backtracking. BFS for shortest path; DFS for exhaustive search and graph properties." },
      { q: "What is dynamic programming?", a: "Dynamic programming solves problems by breaking them into overlapping subproblems, computing each once, and storing results (memoization or tabulation) to avoid redundant work. Key insight: identify optimal substructure (optimal solution contains optimal solutions to subproblems) and overlapping subproblems. Classic problems: Fibonacci, coin change, longest common subsequence, knapsack, matrix chain multiplication." },
      { q: "What is the sliding window technique?", a: "The sliding window maintains a window (subarray or substring) over data and slides it to compute a result without reprocessing elements. Fixes an O(n²) brute-force to O(n). Fixed-size window: track a running sum/count, add the new element, remove the oldest. Variable-size window: expand the right pointer, shrink the left when a constraint is violated. Used for: maximum subarray sum of size k, longest substring without repeating characters." },
      { q: "What is the two-pointer technique?", a: "Two pointers start at different positions and move toward each other (or in the same direction) to solve array/string problems in O(n) instead of O(n²). Common patterns: left and right pointers on a sorted array (two-sum, container with most water, three-sum), fast and slow pointers on a linked list (detect cycle, find middle), and meeting in the middle." },
      { q: "How do you detect a cycle in a linked list?", a: "Floyd's Cycle Detection (Tortoise and Hare): use two pointers, slow (moves 1 step) and fast (moves 2 steps). If they ever meet, a cycle exists. If fast reaches null, no cycle. O(n) time, O(1) space. To find the cycle start: once they meet, reset slow to head and move both one step at a time — they meet again at the cycle's entry point." },
      { q: "What is a heap (priority queue)?", a: "A heap is a complete binary tree satisfying the heap property: in a max-heap, every parent is ≥ its children; min-heap every parent ≤. Insert and delete-min/max are O(log n); peek (find min/max) is O(1). Built using an array where parent of i is at (i-1)/2. Used for: priority queues, Dijkstra's algorithm, heap sort, finding the k-th largest element, and merging k sorted lists." },
      { q: "What is merge sort and why is it preferred for linked lists?", a: "Merge sort recursively splits the array in half, sorts each half, and merges them — O(n log n) time, O(n) space (stable sort). It's preferred for linked lists because it doesn't require random access (unlike quicksort's pivot swapping) — splitting a linked list and merging are natural pointer operations. For arrays, quicksort is usually faster in practice due to better cache performance." },
      { q: "What is quicksort and what is its time complexity?", a: "Quicksort picks a pivot, partitions the array (elements smaller to the left, larger to the right), and recursively sorts each partition. Average case: O(n log n). Worst case: O(n²) with a bad pivot (already sorted array + always picking first/last element). Fix: random pivot selection or median-of-three. In practice, quicksort is the fastest comparison sort due to in-place sorting and cache efficiency." },
      { q: "How do you find the lowest common ancestor (LCA) of two nodes in a binary tree?", a: "Recursive approach: if root is null or equals either node, return root. Recursively search left and right subtrees. If both return non-null, root is the LCA. If only one returns non-null, return that result. O(n) time. For a BST, leverage BST property: if both values are less than root, LCA is in left subtree; if both greater, right subtree; otherwise root is the LCA. O(h) time." },
      { q: "What is a trie (prefix tree) and when is it used?", a: "A trie is a tree where each node represents a character, and paths from root to nodes represent strings. It supports O(m) insert and search (m = string length) and is space-efficient for shared prefixes. Used for: autocomplete, spell checkers, prefix search, longest common prefix, and IP routing tables. More memory-efficient alternatives: compressed tries (Patricia trees) and radix trees." },
      { q: "What is a graph and how is it represented?", a: "A graph G = (V, E) is a set of vertices and edges. Directed (edges have direction) vs undirected; weighted vs unweighted. Representations: Adjacency Matrix (V×V array, O(1) edge lookup, O(V²) space — good for dense graphs), Adjacency List (array of edge lists, O(V+E) space — good for sparse graphs). Most graph problems use adjacency lists." },
      { q: "What is Dijkstra's algorithm?", a: "Dijkstra's finds the shortest path from a source vertex to all other vertices in a graph with non-negative edge weights. It uses a min-heap (priority queue): start with distance 0 for source, infinity for others. Repeatedly extract the minimum-distance vertex, relax its neighbors (update if shorter path found), and push to heap. O((V+E) log V) with a binary heap. Fails with negative edges — use Bellman-Ford instead." },
      { q: "What is the difference between memoization and tabulation?", a: "Both are dynamic programming techniques. Memoization (top-down) uses recursion and caches results in a hash map/array — natural to write, only computes needed subproblems, but has recursion overhead and stack depth limits. Tabulation (bottom-up) fills a table iteratively starting from base cases — no recursion overhead, O(1) space optimisations possible, but may compute subproblems that aren't needed." },
      { q: "How do you find all permutations of a string?", a: "Use backtracking: at each position, swap the current character with each subsequent character, recurse on the remainder, then swap back (backtrack). O(n * n!) time complexity — there are n! permutations each of length n. Alternatively, use a boolean visited array and build permutations by picking unused characters at each step. For unique permutations with duplicates, sort first and skip duplicates in the recursion." },
    ],
  },

  "html-css": {
    title: "HTML & CSS Interview Questions",
    slug: "html-css",
    icon: "🎨",
    metaTitle: "Top 40 HTML & CSS Interview Questions & Answers (2025)",
    metaDesc: "Ace HTML and CSS interviews with 40 top questions on semantic HTML, flexbox, CSS grid, responsive design, specificity, BEM, and animations for 2025.",
    intro: "HTML and CSS fundamentals are tested in every frontend and full-stack interview in India. Whether you're a fresher or experienced developer, these questions on semantic markup, Flexbox, Grid, and responsive design are regularly asked at product companies.",
    related: ["javascript", "react", "typescript"],
    questions: [
      { q: "What is semantic HTML and why does it matter?", a: "Semantic HTML uses elements that convey meaning about their content: <header>, <nav>, <main>, <article>, <section>, <aside>, <footer>, <figure>, <time>. Benefits: (1) Accessibility — screen readers navigate by landmarks. (2) SEO — search engines understand page structure better. (3) Maintainability — code is self-documenting. Avoid overusing <div> and <span> when a semantic element fits." },
      { q: "What is the difference between block, inline, and inline-block elements?", a: "Block elements (div, p, h1-h6, section) take the full available width, start on a new line, and respect width/height/margin/padding on all sides. Inline elements (span, a, strong, em) flow with text, only take as much width as their content, and ignore top/bottom margins and width/height. Inline-block combines both: flows inline but respects width/height and all margins/padding." },
      { q: "What is the CSS Box Model?", a: "Every HTML element is a rectangular box with four layers: content (the actual content), padding (space between content and border), border, and margin (space outside the border). By default (content-box), width and height apply only to the content. box-sizing: border-box includes padding and border in the declared width/height — far more intuitive and now used universally with a * { box-sizing: border-box } reset." },
      { q: "What is CSS specificity and how is it calculated?", a: "Specificity determines which CSS rule wins when multiple rules target the same element. It's calculated as a three-part score: (ID selectors, Class/attribute/pseudo-class selectors, Element/pseudo-element selectors). #id beats .class beats div. !important overrides everything (avoid it). Inline styles beat all selectors. When specificity ties, the last rule in source order wins." },
      { q: "What is the difference between position: relative, absolute, fixed, and sticky?", a: "relative: positioned relative to its normal position; still takes up space in layout. absolute: removed from flow, positioned relative to the nearest positioned (non-static) ancestor. fixed: removed from flow, positioned relative to the viewport — stays in place when scrolling (nav bars). sticky: hybrid — acts as relative until a scroll threshold, then acts as fixed until leaving its scroll container." },
      { q: "What is Flexbox and when would you use it?", a: "Flexbox is a one-dimensional layout system (main axis + cross axis) designed for distributing space among items in a container. Key properties: display:flex, flex-direction (row/column), justify-content (main axis alignment), align-items (cross axis), flex-wrap, flex (shorthand for grow/shrink/basis), gap. Use Flexbox for navigation bars, card rows, centering content, or any 1D layout pattern." },
      { q: "What is CSS Grid and how does it differ from Flexbox?", a: "CSS Grid is a two-dimensional layout system (rows AND columns simultaneously). Define a grid with grid-template-columns, grid-template-rows, and gap. Position items with grid-column and grid-row. Flexbox is 1D — items are distributed along one axis. Rule of thumb: use Grid for page-level layouts (two-dimensional); use Flexbox for component-level layouts (one dimension at a time). They work well together." },
      { q: "What is responsive design and how do you implement it?", a: "Responsive design makes a layout adapt to different screen sizes. Tools: CSS media queries (@media (max-width: 768px) { }), fluid widths (percentages, max-width), relative units (rem, em, vw, vh), flexible images (max-width: 100%), CSS Grid auto-fit/auto-fill with minmax(), and Flexbox wrapping. The mobile-first approach writes base styles for mobile and uses min-width queries to add complexity for larger screens." },
      { q: "What is the difference between em and rem units?", a: "em is relative to the font-size of the element itself (for font-size) or the nearest ancestor with a defined font-size (for other properties). This nesting makes em hard to predict. rem (root em) is always relative to the root element's font-size (html { font-size: 16px } by default). Prefer rem for font sizes (predictable), em for spacing that should scale with local text size." },
      { q: "What is a CSS pseudo-class vs pseudo-element?", a: "Pseudo-classes (:hover, :focus, :nth-child(), :not(), :checked) select elements based on their state or relationship. Pseudo-elements (::before, ::after, ::first-line, ::placeholder) create virtual elements or target specific parts of an element. ::before and ::after insert content without adding DOM nodes — useful for decorative elements. The double colon (::) distinguishes pseudo-elements from pseudo-classes." },
      { q: "What is z-index and stacking context?", a: "z-index controls the stacking order of positioned elements (position other than static). Higher z-index is rendered on top. A stacking context is a conceptual layer where z-index comparisons happen — elements inside a stacking context are z-indexed relative to each other, not the entire page. New stacking contexts are created by: position + z-index, opacity < 1, transform, filter, and several other properties." },
      { q: "What is the difference between visibility: hidden and display: none?", a: "display: none removes the element from the document flow entirely — it takes up no space, is not accessible to screen readers, and cannot receive focus. visibility: hidden makes the element invisible but it still occupies space in the layout. A third option, opacity: 0, makes it invisible but it still occupies space and can receive pointer events (use pointer-events: none if needed)." },
      { q: "What are CSS custom properties (variables)?", a: "CSS custom properties are defined with a -- prefix (--primary-color: #e85a2a) on a selector (usually :root for global scope) and accessed with var(--primary-color). They cascade and can be overridden in any scope. Unlike SASS variables (compiled away), CSS variables are live — you can update them with JavaScript at runtime, enabling dynamic theming without a full re-render." },
      { q: "What is BEM naming convention?", a: "BEM (Block, Element, Modifier) is a CSS class naming methodology: Block is the independent component (.card), Element is a part of the block (.card__title, .card__image — double underscore), Modifier is a state or variant (.card--featured, .card__title--large — double dash). BEM eliminates specificity battles (all classes are equally specific) and makes the relationship between HTML and CSS explicit." },
      { q: "How do CSS animations and transitions work?", a: "Transitions animate a property change between two states smoothly: transition: background-color 0.3s ease. They trigger on state changes (:hover, class additions via JS). Animations (@keyframes) define multi-step sequences with from/to or percentage steps and can loop, delay, and run automatically: animation: spin 1s linear infinite. Transitions are for simple A→B; animations for complex sequences. For performance, animate transform and opacity only (GPU-composited)." },
    ],
  },

  angular: {
    title: "Angular Interview Questions",
    slug: "angular",
    icon: "🔴",
    metaTitle: "Top 40 Angular Interview Questions & Answers (2025)",
    metaDesc: "Prepare for Angular interviews with 40 top questions on components, directives, services, RxJS, dependency injection, and change detection asked in 2025.",
    intro: "Angular is still widely used in enterprise software and large Indian IT companies including TCS, Infosys, Wipro, and Cognizant. These questions on components, directives, RxJS, and dependency injection will prepare you for Angular technical rounds.",
    related: ["typescript", "javascript", "react"],
    questions: [
      { q: "What is Angular and how does it differ from React?", a: "Angular is a full-featured, opinionated framework developed by Google that includes routing, form handling, HTTP client, DI container, and state management conventions out of the box. React is a UI library — you choose your own tools for routing, state, etc. Angular uses TypeScript by default, two-way data binding, and a Component + Module + Service architecture. React is more flexible; Angular gives more structure." },
      { q: "What is a component in Angular?", a: "A component is the fundamental building block of Angular UIs. It consists of a TypeScript class decorated with @Component, an HTML template, and optional CSS styles. The decorator specifies the selector (how to use the component in HTML), templateUrl (or inline template), and styleUrls. Components form a tree with AppComponent as the root." },
      { q: "What is data binding in Angular? What are the four types?", a: "Data binding synchronises data between component class and template. Four types: (1) Interpolation {{value}} — one-way, class to template. (2) Property binding [property]='value' — one-way, class to DOM. (3) Event binding (click)='handler()' — one-way, DOM to class. (4) Two-way binding [(ngModel)]='value' — bidirectional using both property and event binding (the 'banana in a box' syntax)." },
      { q: "What is the difference between ngOnInit and the constructor?", a: "The constructor runs first and is for dependency injection — inject services here. ngOnInit is an Angular lifecycle hook that runs after Angular has initialised the component's inputs. Put initialisation logic (API calls, data processing using @Input values) in ngOnInit, not the constructor. Testing is also easier since you can call ngOnInit explicitly in tests after setting up inputs." },
      { q: "What are Angular directives? What is the difference between structural and attribute directives?", a: "Directives are classes that add behaviour to elements. Structural directives (prefixed with *) alter DOM structure by adding/removing elements: *ngIf, *ngFor, *ngSwitch. Attribute directives change the appearance or behaviour of an element: ngClass, ngStyle, and custom directives using @Directive. The * syntax is shorthand for <ng-template> wrapping." },
      { q: "What is Angular's Dependency Injection system?", a: "Angular has a hierarchical DI system. Services are registered in providers arrays (AppModule for singleton, component-level for scoped instance) or with @Injectable({ providedIn: 'root' }) for tree-shakeable singletons. Angular's injector creates and caches service instances, injecting them via constructor parameters typed to the service class. This enables testability (swap real services for mocks in tests)." },
      { q: "What is RxJS and how is it used in Angular?", a: "RxJS is a library for reactive programming using Observables — streams of data over time. Angular's HttpClient returns Observables; the Router exposes route params as Observables; form value changes are Observables. Key operators: map, filter, switchMap (cancel previous inner Observable), mergeMap, combineLatest, debounceTime, takeUntil. Always unsubscribe to prevent memory leaks (use async pipe or takeUntil with a Subject)." },
      { q: "What is the async pipe in Angular?", a: "The async pipe subscribes to an Observable or Promise in the template and automatically unsubscribes when the component is destroyed, preventing memory leaks. Usage: *ngIf='users$ | async as users'. It also marks the component for change detection when new values arrive. Using async pipe is preferred over manual subscribe/unsubscribe in the component class for template data." },
      { q: "What is Angular Change Detection?", a: "Angular's change detection checks if component data has changed and updates the DOM accordingly. Default strategy: every event, timer, or async operation triggers checking the entire component tree. OnPush strategy: only checks when inputs change (by reference), an event originates from the component, or an Observable emits. OnPush with Observables + async pipe is the performance best practice." },
      { q: "What is the difference between Observable and Promise?", a: "A Promise handles a single future value — it's eager (executes immediately on creation) and not cancellable. An Observable handles zero or more values over time — it's lazy (only executes when subscribed), cancellable (unsubscribe), and composable with operators. Observables support retry, debounce, combine, transform, and many other operators. Use Observables for streams; Promises for single async operations." },
      { q: "What are Angular modules (NgModule)?", a: "NgModule is a decorator that groups related components, directives, pipes, and services. Every Angular app has a root AppModule. Modules declare what components they own, import other modules they depend on, export components for other modules to use, and provide services (in providers). Angular 14 introduced standalone components that don't require a module." },
      { q: "What is lazy loading in Angular routing?", a: "Lazy loading defers loading a feature module's JavaScript bundle until the user navigates to that route. Configure with loadChildren: () => import('./feature/feature.module').then(m => m.FeatureModule) in the route config. This reduces initial bundle size and improves first-load time. Preloading strategies (PreloadAllModules) can background-load lazy modules after the app bootstraps." },
      { q: "What are Angular pipes and how do you create a custom one?", a: "Pipes transform data in templates: {{ date | date:'dd/MM/yyyy' }}, {{ price | currency:'INR' }}, {{ text | uppercase }}. Create a custom pipe with @Pipe({ name: 'myPipe' }) on a class implementing PipeTransform with a transform(value, ...args) method. Declare it in a module. Pure pipes (default) only recalculate when input reference changes; impure pipes recalculate on every change detection cycle." },
      { q: "What is the ViewChild decorator?", a: "@ViewChild lets a component access a child component, directive, or DOM element from the template. Usage: @ViewChild(ChildComponent) child: ChildComponent — after ngAfterViewInit, this.child holds the reference. @ViewChild('myRef') accesses a template reference variable (#myRef). Use for calling methods on child components, reading DOM properties, or accessing form controls." },
      { q: "What is the difference between template-driven and reactive forms in Angular?", a: "Template-driven forms use directives (ngModel, ngForm) in the template — simple, less boilerplate, but limited in dynamic scenarios and harder to test. Reactive forms define the form model in the component class using FormGroup and FormControl — more explicit, testable, dynamic (add/remove controls programmatically), and better for complex validation. Prefer reactive forms for anything beyond a simple contact form." },
    ],
  },

  "hr-interview": {
    title: "HR Interview Questions",
    slug: "hr-interview",
    icon: "🤝",
    metaTitle: "Top 30 HR Interview Questions & Answers for Freshers (2025)",
    metaDesc: "Ace your HR round with the top 30 most-asked HR interview questions and model answers for freshers and experienced candidates in 2025.",
    intro: "The HR round is the final gate before your offer letter. These questions test your attitude, communication, self-awareness, and cultural fit. Indian IT companies like TCS, Infosys, Wipro, Accenture, and startups all follow a similar HR pattern — master these and you'll walk out confident.",
    related: ["behavioral", "aptitude"],
    questions: [
      { q: "Tell me about yourself.", a: "Structure your answer in 3 parts: past (education/background), present (current skills/role or final year project), future (what you want to achieve and how this role fits). Keep it under 2 minutes. Example: 'I'm a Computer Science graduate from XYZ College. I've built strong skills in Java and React through internships and projects. I'm excited about this role because it aligns with my goal of building scalable backend systems.' Never just recite your resume — add personality and direction." },
      { q: "Why do you want to work at our company?", a: "Research the company beforehand. Mention 2-3 specific things: a product you use/admire, their tech stack, their growth trajectory, or their culture. Example: 'I've been following your work on (product/initiative). The engineering challenges you solve at scale — and the culture of ownership I've read about — are exactly the environment where I want to grow. I'm not looking for just any job; I want to build something meaningful.' Generic answers like 'good company' are red flags." },
      { q: "What are your strengths?", a: "Pick 2-3 strengths relevant to the role and back each with a micro-story. For a tech role: 'My biggest strength is breaking down complex problems — in my final year project I reduced API response time by 60% by identifying a single N+1 query. Second, I learn fast — I picked up Docker and CI/CD in two weeks when my internship team needed it.' Avoid generic answers like 'hardworking' without evidence." },
      { q: "What are your weaknesses?", a: "Pick a real weakness that doesn't disqualify you for the role, and always pair it with how you're actively improving. Example: 'I tend to over-engineer solutions — I like thinking about edge cases and scalability even for small features. I've been working on this by setting time-boxes for design decisions and shipping iteratively. It's made me faster without sacrificing quality.' Never say 'I work too hard' — interviewers see through it immediately." },
      { q: "Where do you see yourself in 5 years?", a: "Show ambition that aligns with the company's growth path. Example: 'In 5 years, I want to be a senior engineer who can independently own a product area end-to-end — design, build, and operate it. I'd also like to start mentoring junior developers. I see this company as the place where I can build that depth because of the scale and the kind of problems you solve.' Avoid answers like 'your position' or vague 'growing' statements." },
      { q: "Why should we hire you?", a: "This is your closing pitch. Connect your skills directly to their needs. Example: 'You need someone who can ship production-quality code quickly and communicate well with cross-functional teams. I've done exactly that during my internship — I delivered 3 features end-to-end in 6 weeks, coordinated with design and QA, and wrote zero bugs in prod. I'm technically strong, I take ownership, and I'm excited about this specific team's work.' Be specific and confident, not arrogant." },
      { q: "What are your salary expectations?", a: "Research the market rate on LinkedIn Salary, Glassdoor, or Levels.fyi before the interview. For freshers, state a range: 'Based on my research and the skills I bring, I'm expecting between ₹6–8 LPA. I'm open to discussion based on the full compensation package.' For experienced hires: 'I'm currently at X; I'm targeting Y based on my additional expertise in (skill). I'm flexible if the role has strong growth potential.' Never say 'anything you offer.'" },
      { q: "Tell me about a time you worked in a team and handled a conflict.", a: "Use the STAR method: Situation, Task, Action, Result. Example: 'During our hackathon project, two teammates disagreed on the tech stack — one wanted React, another wanted Vue. I mediated by listing our deadline constraints and everyone's existing skills. We chose React since three of us knew it, cutting the learning curve. We shipped in 24 hours and won second place. I learned that alignment on constraints beats preferences in fast-moving situations.'" },
      { q: "How do you handle pressure and tight deadlines?", a: "Show a system, not just determination. Example: 'I break the work into the smallest deliverable slices, prioritise ruthlessly, and communicate early if something is at risk. During my internship, a production bug surfaced two days before a demo. I stayed focused, identified the root cause within 3 hours by binary-searching the git log, fixed it, and added a regression test. The demo went perfectly. Pressure is manageable when you have a process.'" },
      { q: "Are you willing to relocate or travel for work?", a: "Be honest. If you are willing: 'Yes, I'm open to relocation. I see it as part of building my career and experiencing different work environments.' If not fully: 'I'm open to it for the right opportunity, though I'd appreciate knowing the expected travel frequency so I can plan accordingly.' Never commit to something you can't honour — it creates problems after joining." },
      { q: "What motivates you?", a: "Tie your motivation to the work itself, not just money or stability. Example: 'I'm most motivated when I can see my work directly impact users. During my project, when we saw the app's load time drop from 8 seconds to 1.2 seconds and users started rating it 4.8 stars, that was incredibly satisfying. I want to keep building things that make people's lives meaningfully easier.' Authenticity here matters more than the perfect answer." },
      { q: "Do you have any questions for us?", a: "Always ask 2-3 thoughtful questions — it shows genuine interest. Good questions: 'What does the first 90 days look like for someone joining this team?' / 'What's the biggest technical challenge the team is working through right now?' / 'How does the company support engineers who want to grow into senior roles?' Avoid: 'What is your company's leave policy?' (saves for HR docs) or questions answered on the website." },
      { q: "What is your notice period?", a: "State it accurately. If you're a fresher: 'I'm available to join immediately since I've just completed my degree.' If you're switching jobs: 'My current notice period is 30/60/90 days, but I can discuss buyout options with my current employer if an earlier start date is critical.' Never misrepresent your notice period — it sets up a bad start if discovered." },
      { q: "Why are you leaving your current job?", a: "Keep it professional and forward-looking, never negative. Good answers: 'I've grown a lot in my current role, but the opportunities for the kind of technical depth I'm seeking are limited there. This role seems to offer exactly that.' or 'The team is being restructured and I want to be proactive about finding the right next step.' Never say: 'My manager is terrible' or 'The pay is too low' — even if true, it signals poor professional maturity." },
      { q: "Tell me about your greatest achievement.", a: "Pick something with a measurable outcome. Example: 'My greatest achievement was building a real-time attendance system for my college as a final-year project. It reduced manual attendance errors by 100% and was adopted by 3 departments with 1,200+ students. I designed the architecture, wrote the backend in Node.js, and deployed it on a college server — end-to-end ownership. The Dean commended it in the graduation ceremony.' Quantify whenever possible." },
      { q: "What do you know about our company?", a: "Prepare 3-4 facts: founding story, flagship product/service, recent news (funding, product launch, expansion), and culture highlights. Example: 'You were founded in (year) and are known for (product). I read that you recently (raised Series B / launched in X market / partnered with Y). I also noticed from your tech blog that you use (tech stack), which aligns well with my experience. I'm particularly excited about the (specific team/product area) problems you're solving.' This level of preparation makes you stand out from 80% of candidates." },
      { q: "How would your friends or professors describe you?", a: "This gets at self-awareness and social perception. Example: 'My friends would say I'm the one who always breaks down a problem before reacting — I'm calm under pressure and tend to think before speaking. My professors knew me as someone who would stay after class to ask deeper questions, not just exam-relevant ones. I think both reflect something genuine: I'm genuinely curious and I take quality seriously.'" },
      { q: "Are you interviewing with other companies?", a: "Be honest but strategically vague. 'Yes, I'm exploring a few opportunities to make an informed decision. However, (this company/role) is my top priority because of (specific reason — team, tech, product). If things move forward here, I'd be happy to share my timeline.' This creates mild urgency without dishonesty or desperation." },
      { q: "How do you keep your technical skills up to date?", a: "Show active habits, not passive intentions. Example: 'I follow a few engineering blogs like the Netflix Tech Blog and High Scalability. I spend about 2–3 hours a week doing LeetCode problems to keep my algorithms sharp. I recently completed a course on System Design and I'm building a side project with (tech) to apply what I learned. Staying current feels like part of the job, not a chore.'" },
      { q: "Tell me about a challenge you faced and how you overcame it.", a: "Use STAR. Pick a real technical or collaborative challenge. Example: 'In my internship, I was tasked with migrating a legacy REST API to GraphQL in 3 weeks — a technology I'd never used. I spent the first 3 days solely on reading docs and building a prototype. By day 7 I had the first endpoint working. I documented every decision so the team could review. We delivered on time and the team adopted GraphQL for 2 more services after seeing the results. The lesson: break unfamiliar problems into small experiments before writing production code.'" },
    ],
  },

  dbms: {
    title: "DBMS Interview Questions",
    slug: "dbms",
    icon: "🗄️",
    metaTitle: "Top 30 DBMS Interview Questions & Answers for Freshers (2025)",
    metaDesc: "Crack your DBMS interview with the top 30 most-asked database management system questions covering normalization, ACID, indexing, joins, and transactions for 2025.",
    intro: "DBMS is a core subject asked in every TCS, Infosys, Wipro, Cognizant, and campus placement interview. Whether you're appearing for a tech or BTECH role, these questions on normalization, ACID properties, indexing, and transactions are non-negotiable to master.",
    related: ["sql", "system-design"],
    questions: [
      { q: "What is a DBMS? How is it different from a file system?", a: "A Database Management System (DBMS) is software that manages, stores, and retrieves structured data efficiently. Unlike a file system, a DBMS provides: data integrity enforcement, concurrent access control, query language (SQL), role-based security, crash recovery, and elimination of data redundancy. File systems store raw files with no built-in relationships, constraints, or query capability — you'd have to write custom code for everything a DBMS gives you out of the box." },
      { q: "What is the difference between DBMS and RDBMS?", a: "A DBMS stores data without enforcing relationships between tables (e.g., file-based or hierarchical databases like IMS). An RDBMS (Relational DBMS) organises data into tables with defined relationships enforced via primary keys, foreign keys, and referential integrity constraints. RDBMS follows E.F. Codd's 12 rules for relational databases. Examples: MySQL, PostgreSQL, Oracle, SQL Server are RDBMS; MongoDB (document store) is a NoSQL DBMS." },
      { q: "What are ACID properties?", a: "ACID ensures reliable database transactions: Atomicity — a transaction is all-or-nothing (either all operations succeed or none are committed); Consistency — a transaction brings the database from one valid state to another, never violating constraints; Isolation — concurrent transactions execute as if they were sequential (no dirty reads, phantom reads); Durability — once committed, data survives system crashes (written to disk/WAL). ACID is the foundation of transactional integrity in RDBMS." },
      { q: "What is normalization? What are 1NF, 2NF, and 3NF?", a: "Normalization eliminates redundancy and ensures data integrity by organising tables according to normal forms. 1NF: Each column must contain atomic (indivisible) values and each row must be unique — no repeating groups or arrays. 2NF: Must be in 1NF + every non-key attribute must depend on the entire primary key (no partial dependencies — only matters when the key is composite). 3NF: Must be in 2NF + no transitive dependencies (non-key columns must not depend on other non-key columns). BCNF (Boyce-Codd): stricter 3NF — every determinant must be a candidate key." },
      { q: "What is denormalization and when would you use it?", a: "Denormalization intentionally introduces redundancy into a normalized schema to improve read performance. By storing pre-joined data, you eliminate costly JOIN operations at query time. Use it in read-heavy analytics systems, reporting tables, or data warehouses where writes are infrequent but queries are complex. Example: storing customer_name in an Orders table even though it exists in Customers — saves a JOIN on every order query. The tradeoff: updates become harder (must update in multiple places)." },
      { q: "What is a primary key, foreign key, and unique key?", a: "Primary Key (PK): uniquely identifies each row in a table, cannot be NULL, one per table. Foreign Key (FK): a column in one table that references the PK of another table — enforces referential integrity (you can't insert an order for a customer_id that doesn't exist). Unique Key: enforces uniqueness like PK but allows one NULL value and a table can have multiple unique keys. Example: email column in Users table can be a unique key." },
      { q: "What is a transaction? What are its states?", a: "A transaction is a logical unit of work consisting of one or more SQL operations that must all succeed or all fail together. Transaction states: Active (executing), Partially Committed (last statement executed, awaiting commit), Committed (all changes made permanent), Failed (error occurred, rollback needed), Aborted (rolled back, database restored to pre-transaction state). Use BEGIN TRANSACTION, COMMIT, ROLLBACK to control transactions in SQL." },
      { q: "What are the different types of JOIN in SQL?", a: "INNER JOIN: returns only rows where there's a match in both tables. LEFT JOIN: returns all rows from the left table plus matched rows from the right (NULLs for no match). RIGHT JOIN: all rows from the right + matched from left. FULL OUTER JOIN: all rows from both tables with NULLs where no match. CROSS JOIN: Cartesian product — every combination of rows. SELF JOIN: joins a table to itself (e.g., finding employees with the same manager). NATURAL JOIN: auto-joins on columns with the same name." },
      { q: "What is indexing in DBMS? What are its types?", a: "An index is a data structure (typically a B-tree or hash) that speeds up data retrieval at the cost of additional storage and slower writes. Types: Primary Index — on the primary key, data file is physically sorted by it. Secondary Index — on non-key attributes, data file is NOT sorted by it (also called non-clustered). Clustered Index — determines the physical order of data on disk; a table can have only one. Non-Clustered Index — a separate structure pointing to row locations; a table can have many. Dense Index — entry for every search key. Sparse Index — entry for only some search keys (in a sorted file)." },
      { q: "What is the difference between clustered and non-clustered index?", a: "Clustered index determines the physical sort order of data in the table — the table IS the index. A table can have only one clustered index (usually the PK). Data retrieval is fastest for range queries on the clustered key. Non-clustered index is a separate B-tree structure containing the indexed column(s) plus a pointer (row locator) to the actual data row. A table can have many non-clustered indexes. Non-clustered is slower than clustered but more flexible." },
      { q: "What is a deadlock in DBMS? How can it be prevented?", a: "A deadlock occurs when two or more transactions are each waiting for the other to release a lock, creating a circular wait. Example: T1 holds lock on A and wants B; T2 holds lock on B and wants A — both wait forever. Prevention strategies: Lock Ordering (always acquire locks in the same order), Timeouts (abort a transaction that waits too long), Wait-Die / Wound-Wait schemes (priority-based preemption), or Deadlock Detection (let it happen, then abort one transaction using a cycle-detection algorithm)." },
      { q: "What are the different types of database locks?", a: "Shared Lock (S): allows multiple transactions to read the same data simultaneously; no writes allowed while shared locks exist. Exclusive Lock (X): only one transaction can hold it; blocks all other reads and writes. Intent Locks: signal to higher-level locks that a lower-level lock will be acquired (Intent Shared IS, Intent Exclusive IX). Two-Phase Locking (2PL): growing phase (acquire locks), shrinking phase (release locks) — guarantees serializability but can deadlock." },
      { q: "What is concurrency control? What are dirty read, non-repeatable read, and phantom read?", a: "Concurrency control ensures correct execution of simultaneous transactions. Dirty Read: reading data written by an uncommitted transaction (which might be rolled back). Non-Repeatable Read: reading the same row twice within one transaction gets different values (another transaction updated/deleted it between reads). Phantom Read: re-running a query returns different rows (another transaction inserted/deleted rows matching the WHERE clause). SQL isolation levels — Read Uncommitted, Read Committed, Repeatable Read, Serializable — progressively prevent more of these anomalies." },
      { q: "What is a view in SQL?", a: "A view is a virtual table based on a SELECT query stored in the database. It doesn't store data itself — it queries the underlying tables on every access. Uses: simplify complex queries (define once, use by name), security (expose only certain columns to users), and abstraction (hide schema changes). Updatable views allow INSERT/UPDATE/DELETE if they map to a single base table with no aggregation. Materialized views (in PostgreSQL, Oracle) physically store the query result and can be refreshed periodically for performance." },
      { q: "What is the difference between DELETE, TRUNCATE, and DROP?", a: "DELETE: DML command, removes rows one by one with a WHERE clause, writes to transaction log (can be rolled back), triggers fire, slower. TRUNCATE: DDL command, removes ALL rows at once without WHERE, minimal logging (much faster), cannot be rolled back in most RDBMS (in PostgreSQL it can), triggers do NOT fire, resets identity columns. DROP: DDL command, deletes the entire table structure and all data permanently — cannot be rolled back. Think: DELETE removes content, TRUNCATE empties the container, DROP destroys the container." },
      { q: "What is a stored procedure and how is it different from a function?", a: "A stored procedure is a precompiled set of SQL statements stored in the database, executed with CALL or EXEC. It can perform DML, DDL, call other procedures, and return multiple result sets. A function must return a single value (scalar) or a table, can be used inside SELECT statements, and cannot perform DML (in most RDBMS). Key differences: functions are used in expressions, procedures are called standalone; functions cannot have side effects (ideally); procedures can have OUT parameters, transactions, and error handling." },
      { q: "What is a trigger?", a: "A trigger is a stored procedure that automatically executes in response to specific DML events (INSERT, UPDATE, DELETE) on a table. Types: BEFORE trigger (runs before the operation — useful for validation/transformation), AFTER trigger (runs after — useful for audit logging, cascading actions), INSTEAD OF trigger (replaces the operation — used on views). Example: an AFTER INSERT trigger on Orders could automatically update inventory in a Products table. Use triggers sparingly — they add hidden logic that's hard to debug." },
      { q: "What is an ER diagram?", a: "An Entity-Relationship (ER) diagram is a visual representation of data and the relationships between entities. Key components: Entities (rectangles) — real-world objects like Student, Course. Attributes (ellipses) — properties of entities (name, ID). Relationships (diamonds) — associations between entities (enrolled-in). Cardinality: one-to-one (1:1), one-to-many (1:N), many-to-many (M:N). ER diagrams are the first step in database design — used to plan the schema before writing SQL." },
      { q: "What is the difference between SQL and NoSQL?", a: "SQL (Relational): structured tables with fixed schema, ACID transactions, powerful JOIN/aggregation queries, scales vertically. Best for: financial systems, ERP, apps requiring complex queries and strong consistency. NoSQL: flexible/schema-less (document, key-value, column, graph stores), BASE consistency model, horizontal scaling, often eventual consistency. Best for: high-write workloads, unstructured data, real-time apps, social graphs. Examples: MongoDB (document), Redis (key-value), Cassandra (column), Neo4j (graph). Choose based on data structure, consistency needs, and scale." },
      { q: "What is functional dependency?", a: "A functional dependency X → Y means that the value of X uniquely determines the value of Y. Example: StudentID → StudentName means knowing the StudentID tells you the StudentName. Trivial dependency: Y is a subset of X (X → X is always true). Non-trivial: Y is not a subset of X. Functional dependencies drive normalization — 2NF eliminates partial dependencies (non-key → part of composite key); 3NF eliminates transitive dependencies (non-key → non-key → key)." },
      { q: "What is a checkpoint in DBMS?", a: "A checkpoint is a mechanism where the DBMS writes all dirty (modified but uncommitted) pages from the buffer to disk and records the current state in the log. Purpose: speed up crash recovery — on restart, instead of replaying the entire log, the system only replays transactions after the last checkpoint. Types: Sharp checkpoint (halts all writes briefly), Fuzzy checkpoint (allows concurrent writes). Checkpoints are why databases can recover quickly from crashes without losing committed data." },
    ],
  },

  "operating-system": {
    title: "Operating System Interview Questions",
    slug: "operating-system",
    icon: "💻",
    metaTitle: "Top 30 Operating System Interview Questions for Freshers (2025)",
    metaDesc: "Master OS interview questions on processes, threads, scheduling, deadlock, memory management, and virtual memory for campus placements and tech interviews in 2025.",
    intro: "Operating Systems is one of the most commonly tested CS fundamentals in campus placements at TCS, Infosys, Amazon, Microsoft, and every company that does core technical rounds. These 30 questions cover processes, scheduling, deadlock, memory management, and synchronization — exactly what interviewers ask.",
    related: ["computer-networks", "system-design"],
    questions: [
      { q: "What is an Operating System? What are its main functions?", a: "An OS is system software that acts as an interface between hardware and user applications. Main functions: Process Management (create, schedule, terminate processes), Memory Management (allocate/deallocate RAM), File System Management (organize files on disk), I/O Management (manage device drivers), Security & Access Control, and providing a User Interface (CLI/GUI). Without an OS, each application would need to directly manage hardware — impractical and insecure." },
      { q: "What is the difference between a process and a thread?", a: "A process is an independent program in execution with its own memory space (code, data, heap, stack). Threads are lightweight units of execution within a process — they share the same memory space (code, data, heap) but each has its own stack and registers. Creating a thread is much cheaper than creating a process. Context switching between threads is faster. Processes are isolated; a crash in one doesn't affect another. Threads within a process can corrupt shared memory if not synchronized properly." },
      { q: "What is CPU scheduling? What are the main scheduling algorithms?", a: "CPU scheduling decides which process gets the CPU when multiple processes are ready. Key algorithms: FCFS (First Come First Served) — simple but poor for short jobs behind long ones. SJF (Shortest Job First) — optimal average wait time but requires knowing burst time in advance. Round Robin — each process gets a fixed time quantum, cycled repeatedly — best for time-sharing. Priority Scheduling — highest priority runs first, can cause starvation (fixed with aging). Multilevel Queue — separate queues for different process types (interactive vs batch)." },
      { q: "What is a deadlock? What are the four necessary conditions?", a: "A deadlock is a situation where a set of processes are all waiting for resources held by each other — none can proceed. The four Coffman conditions (all must hold simultaneously): 1. Mutual Exclusion — at least one resource is non-shareable. 2. Hold and Wait — a process holds resources while waiting for more. 3. No Preemption — resources cannot be forcibly taken from a process. 4. Circular Wait — a circular chain of processes each waiting for a resource held by the next. Deadlock prevention removes at least one condition; deadlock avoidance uses algorithms like Banker's Algorithm." },
      { q: "What is virtual memory? How does it work?", a: "Virtual memory creates an illusion that each process has its own large, contiguous address space, even if physical RAM is limited. It works by mapping virtual addresses to physical RAM pages using a page table maintained by the OS and Memory Management Unit (MMU). Pages not currently in RAM are stored on disk (swap space). When a process accesses a page not in RAM, a page fault occurs and the OS loads the page from disk. This allows running programs larger than physical RAM but disk access causes latency." },
      { q: "What is paging? How does it differ from segmentation?", a: "Paging divides physical memory into fixed-size frames and virtual memory into equal-size pages. The page table maps virtual page numbers to physical frame numbers. It eliminates external fragmentation (frames are fixed-size) but causes internal fragmentation (last page may not be full). Segmentation divides memory into variable-size logical units (code, stack, heap segments) based on program structure. It has no internal fragmentation but suffers external fragmentation. Modern OS (x86-64) use paged segmentation — segmentation on top of paging." },
      { q: "What is a page fault? What happens when one occurs?", a: "A page fault occurs when a process accesses a virtual page not currently loaded in physical RAM. Steps: 1. MMU detects the missing page and raises a page fault interrupt. 2. OS takes control, checks if the access is valid (if not → segfault). 3. OS finds a free frame (or evicts a page using a replacement algorithm like LRU). 4. OS reads the required page from disk into the frame. 5. OS updates the page table. 6. Process resumes at the faulting instruction. Frequent page faults (thrashing) kill performance." },
      { q: "What is thrashing?", a: "Thrashing occurs when a system spends more time swapping pages in and out of memory than executing actual processes. It happens when the total working set of all running processes exceeds available physical RAM — each process page-faults constantly, causing the OS to continuously swap, leaving no CPU time for real work. Solutions: reduce multiprogramming degree (run fewer processes), increase RAM, improve page replacement algorithm, or use working set model to track how many pages each process actually needs." },
      { q: "What is a semaphore? How does it differ from a mutex?", a: "A semaphore is a synchronization primitive that controls access to shared resources. It maintains a counter: wait() (P) decrements it; signal() (V) increments it. Binary semaphore (0 or 1) — works like a mutex. Counting semaphore — allows N processes into a critical section simultaneously. Mutex (Mutual Exclusion Lock): a locking mechanism where only the thread that locked it can unlock it — it has ownership. A binary semaphore has no ownership — any thread can signal it. Use mutex for mutual exclusion; use semaphore for signaling between threads or resource counting." },
      { q: "What is a critical section? What are Peterson's Solution conditions?", a: "A critical section is a code segment that accesses shared resources and must not be executed by more than one process simultaneously. Requirements for a correct critical section solution: 1. Mutual Exclusion — only one process inside at a time. 2. Progress — if no process is in the critical section, a waiting process should be able to enter without indefinite delay. 3. Bounded Waiting — a process must not wait forever (bounded number of times others can enter before it). Peterson's Solution uses two variables (flag and turn) to satisfy all three for two processes — not used in practice (hardware solutions preferred)." },
      { q: "What is context switching?", a: "Context switching is the process of saving the state (context) of the currently running process and restoring the context of the next process to run. The context includes: CPU registers, program counter, stack pointer, process state, memory mappings. Context switch overhead is pure overhead — no useful work is done during the switch. Threads switch faster than processes because they share the same virtual address space (no need to flush TLB). Context switches happen due to interrupts, system calls, or the scheduler's time quantum expiring." },
      { q: "What are the different states of a process?", a: "The typical process state model: New — process is being created. Ready — process is in memory, waiting for CPU. Running — process is currently executing on the CPU. Waiting/Blocked — process is waiting for an I/O event or resource (not using CPU). Terminated — process has finished execution. Transitions: New → Ready (admitted), Ready → Running (scheduler dispatch), Running → Waiting (I/O request), Waiting → Ready (I/O completion), Running → Ready (preempted/time quantum), Running → Terminated (exit)." },
      { q: "What is Inter-Process Communication (IPC)?", a: "IPC allows processes to communicate and synchronize. Methods: Shared Memory — fastest, processes map the same memory region (requires synchronization). Message Passing — processes send/receive messages via OS (safer, no shared state). Pipes — unidirectional data stream between parent-child processes. Named Pipes (FIFOs) — like pipes but work between unrelated processes. Sockets — IPC over a network or locally. Signals — asynchronous notifications (e.g., SIGTERM, SIGKILL). Message Queues — persistent message buffers managed by OS. Semaphores — for synchronization, not data transfer." },
      { q: "What is the difference between preemptive and non-preemptive scheduling?", a: "Preemptive scheduling: the OS can forcibly remove a process from the CPU mid-execution (e.g., when its time quantum expires or a higher-priority process arrives). Examples: Round Robin, Priority Preemptive. Better for interactive systems — ensures responsiveness. Non-preemptive (cooperative) scheduling: once a process has the CPU, it runs until it voluntarily gives it up (I/O request, system call, or completion). Examples: FCFS, SJF (non-preemptive). Simpler but can cause poor response time if a process hogs the CPU." },
      { q: "What is memory fragmentation? What are its types?", a: "Fragmentation is wasted memory that cannot be used effectively. Internal Fragmentation: allocated memory block is larger than required — the unused space inside the block is wasted. Occurs in fixed-size allocation (paging). Example: process needs 13KB but gets a 16KB page — 3KB wasted. External Fragmentation: enough total free memory exists but it's scattered in non-contiguous pieces, so large requests can't be satisfied. Occurs in variable-size allocation (segmentation). Solution: compaction (moving processes to consolidate free space) or paging (fixed-size blocks eliminate external fragmentation)." },
      { q: "What is a system call?", a: "A system call is the programmatic way a user-space application requests a service from the OS kernel. When an app needs hardware access, file operations, process creation, or network I/O, it makes a system call which switches the CPU from user mode to kernel mode. Examples: fork() (create process), exec() (run a program), open()/read()/write() (file I/O), socket() (networking), malloc() uses brk()/mmap() internally. System calls are expensive relative to regular function calls due to the mode switch and context save." },
      { q: "What is the difference between kernel mode and user mode?", a: "Kernel (privileged) mode: CPU has unrestricted access to all hardware — can execute any instruction, access any memory, manage I/O. OS kernel runs in this mode. User mode: CPU has restricted access — cannot directly access hardware, protected memory, or execute privileged instructions. User applications run in this mode. The separation protects the OS and other processes from buggy/malicious code. Switching from user to kernel mode happens via system calls, hardware interrupts, or exceptions — the hardware enforces this boundary." },
      { q: "What are page replacement algorithms?", a: "When physical RAM is full and a new page must be loaded, a page replacement algorithm selects which existing page to evict: FIFO (First In First Out): evicts the oldest page — simplest but can exhibit Bélády's anomaly (more frames → more faults). Optimal: evicts the page that won't be used for the longest time in future — theoretical benchmark, impossible in practice. LRU (Least Recently Used): evicts the page not used for the longest time — good approximation of Optimal, requires tracking access times. Clock/Second Chance: efficient LRU approximation using a circular buffer with reference bits." },
      { q: "What is the Banker's Algorithm?", a: "Banker's Algorithm is a deadlock avoidance algorithm. The OS maintains: Max (maximum resources each process may need), Allocation (currently allocated), Need (Max - Allocation), and Available (free resources). Before granting a resource request, the OS checks if the resulting state is 'safe' — i.e., there exists a sequence (safe sequence) in which all processes can complete using available resources. If safe, the request is granted; if not, the process must wait. Named after a bank that only lends money if it can still satisfy all customers' maximum withdrawal needs." },
      { q: "What is spooling in OS?", a: "Spooling (Simultaneous Peripheral Operations On-Line) is a technique where data is buffered in a disk area (spool) for a device (like a printer) that can't keep up with the request rate. Instead of sending data directly to a slow device, the OS writes it to a spool on disk; a background daemon (spooler) feeds the spool to the device at its own pace. This lets multiple processes 'print' simultaneously without waiting for the physical printer — each process's print job is queued. Spooling is also used for batch job scheduling." },
    ],
  },

  "computer-networks": {
    title: "Computer Networks Interview Questions",
    slug: "computer-networks",
    icon: "🌐",
    metaTitle: "Top 30 Computer Networks Interview Questions for Freshers (2025)",
    metaDesc: "Ace your Computer Networks interview with the top 30 questions on OSI model, TCP/IP, DNS, HTTP, routing, and network security for campus placements in 2025.",
    intro: "Computer Networks is tested in every core CS interview at TCS, Infosys, Wipro, Amazon, and Cisco. Whether you're applying for software engineering or networking roles, these questions on OSI layers, TCP vs UDP, DNS, HTTP, and routing are asked repeatedly at every level.",
    related: ["operating-system", "system-design"],
    questions: [
      { q: "What is the OSI model? Name all 7 layers.", a: "The OSI (Open Systems Interconnection) model is a conceptual framework for how network communication works. 7 layers (top to bottom): 7. Application (HTTP, FTP, SMTP — user-facing protocols). 6. Presentation (encryption, encoding, compression — SSL/TLS). 5. Session (establish, manage, terminate sessions). 4. Transport (end-to-end delivery, segmentation — TCP/UDP). 3. Network (logical addressing, routing — IP, ICMP). 2. Data Link (MAC addressing, framing, error detection — Ethernet, Wi-Fi). 1. Physical (bits over wire/wireless — cables, signals). Mnemonic: 'All People Seem To Need Data Processing.'" },
      { q: "What is the TCP/IP model and how does it map to OSI?", a: "TCP/IP is the practical 4-layer model used in the internet. Application layer (maps to OSI 5+6+7 — HTTP, DNS, FTP, SMTP). Transport layer (OSI 4 — TCP, UDP). Internet layer (OSI 3 — IP, ICMP, ARP). Network Access/Link layer (OSI 1+2 — Ethernet, Wi-Fi). TCP/IP is descriptive (what the internet actually does); OSI is prescriptive (a reference model). All real internet communication uses TCP/IP — OSI is used for understanding and troubleshooting." },
      { q: "What is the difference between TCP and UDP?", a: "TCP (Transmission Control Protocol): connection-oriented (three-way handshake), reliable (guarantees delivery, ordering, no duplicates), flow control, congestion control, slower. Use for: HTTP/HTTPS, email, file transfer, databases. UDP (User Datagram Protocol): connectionless, unreliable (no guarantee of delivery, order, or deduplication), no handshake, much lower overhead, faster. Use for: DNS (fast lookups), video streaming, online gaming, VoIP — where speed matters more than perfection. UDP packets are called datagrams." },
      { q: "What is the TCP three-way handshake?", a: "The three-way handshake establishes a TCP connection before data transfer: 1. SYN: client sends a segment with SYN flag set and a random initial sequence number (ISN) to the server. 2. SYN-ACK: server responds with SYN+ACK flags, acknowledging the client's ISN+1 and sending its own ISN. 3. ACK: client sends ACK acknowledging the server's ISN+1. Now both sides know each other's sequence numbers and the connection is established. Connection teardown uses a four-way handshake (FIN, ACK, FIN, ACK)." },
      { q: "What is an IP address? What is the difference between IPv4 and IPv6?", a: "An IP address is a logical identifier assigned to a device on a network, used for routing packets. IPv4: 32-bit, written as 4 decimal octets (192.168.1.1), supports ~4.3 billion addresses — exhausted in 2011. IPv6: 128-bit, written as 8 groups of 4 hex digits (2001:0db8:85a3::8a2e:0370:7334), supports ~3.4×10³⁸ addresses, built-in IPsec, simpler header, no broadcast (uses multicast). IPv6 adoption is growing but many networks still use IPv4 with NAT to extend address space." },
      { q: "What is DNS and how does it work?", a: "DNS (Domain Name System) translates human-readable domain names (google.com) to IP addresses. Resolution steps: 1. Browser checks its cache. 2. OS checks its cache + /etc/hosts. 3. Query sent to Recursive Resolver (ISP or 8.8.8.8). 4. Resolver queries Root Name Server (knows TLD servers). 5. Resolver queries TLD Name Server (.com server — knows authoritative NS). 6. Resolver queries Authoritative Name Server (returns the actual IP). 7. IP cached at each level with TTL. DNS uses UDP on port 53 (TCP for large responses or zone transfers)." },
      { q: "What is the difference between a hub, switch, and router?", a: "Hub (Layer 1): broadcasts all data to all ports — no intelligence, creates collisions, obsolete. Switch (Layer 2): uses MAC address table to forward frames only to the correct port — no collisions, creates separate collision domains. Router (Layer 3): routes packets between different IP networks using IP addresses and routing tables — connects your LAN to the internet, performs NAT. Summary: hub broadcasts blindly, switch forwards intelligently within a LAN, router connects different networks and makes forwarding decisions based on IP." },
      { q: "What is NAT (Network Address Translation)?", a: "NAT translates private IP addresses (192.168.x.x, 10.x.x.x) to one public IP address for internet communication, and vice versa for return traffic. This extends IPv4 address space — thousands of devices can share one public IP. The router maintains a NAT table mapping (private IP:port ↔ public IP:port). Types: Static NAT (one-to-one), Dynamic NAT (pool of public IPs), PAT/NAPT (many-to-one using ports — most common home router setup). Downside: NAT breaks true end-to-end connectivity (problems for P2P, VoIP, gaming)." },
      { q: "What is the difference between HTTP and HTTPS?", a: "HTTP (HyperText Transfer Protocol): application-layer protocol for transferring web content, port 80, plaintext — all data is visible to anyone intercepting traffic. HTTPS: HTTP over TLS (Transport Layer Security), port 443, encrypted. TLS uses asymmetric encryption (RSA/ECDH) to exchange a symmetric key, then encrypts all traffic with that key. HTTPS prevents eavesdropping, tampering, and man-in-the-middle attacks. TLS also authenticates the server via a digital certificate signed by a Certificate Authority. Always use HTTPS — Chrome now marks HTTP sites as 'Not Secure.'" },
      { q: "What are HTTP methods (verbs)?", a: "GET: retrieve a resource (no body, idempotent, cacheable). POST: submit data to create a resource (has body, not idempotent). PUT: replace a resource entirely (idempotent). PATCH: partial update of a resource (not necessarily idempotent). DELETE: remove a resource (idempotent). HEAD: like GET but returns only headers, no body — used to check if resource exists. OPTIONS: returns supported methods for a URL (used in CORS preflight). Idempotent means calling it multiple times has the same effect as calling it once." },
      { q: "What is a MAC address?", a: "A MAC (Media Access Control) address is a 48-bit hardware identifier burned into every network interface card (NIC) at the factory. Written as 6 hex pairs (AA:BB:CC:DD:EE:FF). The first 3 bytes identify the manufacturer (OUI — Organizationally Unique Identifier); the last 3 are unique to the device. MAC addresses operate at Layer 2 (Data Link) — used for local network delivery. Unlike IP addresses, MACs don't change with network topology (though they can be spoofed in software). ARP translates IP addresses to MAC addresses within a LAN." },
      { q: "What is ARP (Address Resolution Protocol)?", a: "ARP resolves IP addresses to MAC addresses within a local network. Process: when a device wants to send a packet to 192.168.1.5 but doesn't know its MAC, it broadcasts an ARP Request ('Who has 192.168.1.5?') to all devices on the LAN. The device with that IP replies with an ARP Reply ('I have 192.168.1.5, my MAC is XX:XX:XX:XX:XX:XX'). The requester caches this mapping in its ARP table (ARP cache). ARP Spoofing: attacker sends fake ARP replies to poison ARP caches — used for man-in-the-middle attacks." },
      { q: "What is subnetting and what is a subnet mask?", a: "Subnetting divides a large network into smaller sub-networks (subnets) for better organization, security, and IP efficiency. A subnet mask defines which part of an IP address is the network and which is the host. Example: IP 192.168.1.50, mask 255.255.255.0 (/24) — first 3 octets are network (192.168.1), last octet is host (50). CIDR notation: /24 means 24 bits for network, 8 for hosts (254 usable addresses). Subnetting shrinks broadcast domains, isolates security zones, and allows efficient IP allocation." },
      { q: "What is a firewall?", a: "A firewall is a network security device (hardware or software) that monitors and filters incoming/outgoing traffic based on predefined security rules. Types: Packet Filter (inspects IP/port headers, stateless). Stateful Firewall (tracks connection state, knows if a packet belongs to an established session). Application/Proxy Firewall (deep packet inspection, understands application protocols). NGFW (Next-Gen): combines stateful + DPI + IDS/IPS + SSL inspection. Rules are evaluated top-to-bottom; first match wins. A DMZ (demilitarized zone) uses two firewalls to isolate public-facing servers." },
      { q: "What is a VPN?", a: "A VPN (Virtual Private Network) creates an encrypted tunnel over a public network (internet) between two endpoints, making remote connections appear as if they're on a private network. Use cases: remote work (employee connects to company network securely), bypass geo-restrictions, privacy from ISP. Types: Site-to-site VPN (connects entire networks), Remote access VPN (individual user to network), SSL VPN (browser-based). Protocols: OpenVPN, WireGuard, IPSec/IKEv2, L2TP. VPN encrypts data in transit but the VPN provider can see your traffic — zero trust models are replacing traditional VPNs." },
      { q: "What is the difference between bandwidth, throughput, and latency?", a: "Bandwidth: the maximum data transmission capacity of a link — like the width of a pipe (e.g., 1 Gbps Ethernet). Throughput: actual data successfully transferred per unit time — what you really get (lower than bandwidth due to overhead, errors, congestion). Latency (RTT): the time for a packet to travel from source to destination and back. High bandwidth doesn't mean low latency — a satellite link can have 50 Mbps but 600ms latency. Low latency matters for real-time apps (gaming, VoIP); high throughput matters for file transfer and streaming." },
      { q: "What is DHCP?", a: "DHCP (Dynamic Host Configuration Protocol) automatically assigns IP addresses and network configuration to devices when they join a network. DORA process: Discover (client broadcasts 'I need an IP'), Offer (DHCP server offers an IP), Request (client accepts the offer), Acknowledge (server confirms the lease). DHCP also provides: subnet mask, default gateway, DNS server addresses, lease duration. Without DHCP, every device would need a manually configured static IP. DHCP server is usually the router on home networks, or a dedicated server in enterprises." },
      { q: "What is SSL/TLS and how does the TLS handshake work?", a: "TLS (Transport Layer Security) is the cryptographic protocol securing HTTPS, email, and other network communications. TLS 1.3 handshake: 1. Client Hello (supported TLS version, cipher suites, random nonce). 2. Server Hello (chosen cipher suite, its certificate, server random). 3. Client verifies the server certificate against trusted CAs. 4. Key Exchange (ECDH): both sides compute the same shared secret without transmitting it. 5. Session keys derived from shared secret + randoms. 6. Finished messages verify the handshake wasn't tampered with. All subsequent data encrypted with symmetric keys (AES-GCM). TLS 1.3 reduced handshake from 2 round trips to 1." },
      { q: "What is the difference between a socket and a port?", a: "A port is a 16-bit number (0–65535) that identifies a specific process or service on a host (HTTP = 80, HTTPS = 443, SSH = 22). Ports allow multiplexing — multiple services run on the same IP. A socket is an endpoint for communication — the combination of IP address + port + protocol (TCP or UDP). A socket pair (client socket + server socket) uniquely identifies a connection. In code, a socket is a file descriptor you read/write to communicate over the network. Well-known ports: 0–1023 (requires root). Registered: 1024–49151. Ephemeral: 49152–65535." },
      { q: "What is a proxy server? How is it different from a reverse proxy?", a: "Forward Proxy: sits between clients and the internet, forwarding requests on behalf of clients. Clients are aware of it. Used for: internet access control, caching, anonymity (hide client IP from servers). Reverse Proxy: sits in front of backend servers, forwarding requests from clients on behalf of servers. Clients are usually unaware of it. Used for: load balancing, SSL termination, caching, DDoS protection, API gateway. Examples: Nginx and HAProxy are common reverse proxies. Cloudflare is a reverse proxy + CDN. Your corporate web filter is a forward proxy." },
    ],
  },
};

export const ALL_TECHS = Object.values(TECHS).sort((a, b) => a.title.localeCompare(b.title));
